# Бэклог

Порядок — **по тому, что сильнее бьёт по глазу на картоне**, а не по величине числа.
Каждый пункт: в чём проблема, чем подтверждается, где в коде.

Цифры сняты 2026-09-04 стендом `tools/MosaicGenerator.Diag` по восьми фото из `samples/`
на размерах 15×15, A4 (21×29,7) и 30×30 см, откус 10 мм, 12 цветов. Разбор укладчика —
`docs/kursy-obryvayutsya-plan.md` (стенд с флагом `--why`).

**Судить по картону, а не по метрике** (правило в `CLAUDE.md`). Главный дефект зависит от
сюжета, и это определило порядок ниже:

| фото | что бросается в глаза первым |
|---|---|
| gull, gull-2, dolphin | фон рябит клиньями (п. 1); внутри силуэта крошево (п. 3) |
| human | фон терпимо, **лицо не читается** — деталь мельче куска (п. 3) |
| landscape | половина панно — небо крошевом без курсов (п. 3) |
| **landscape-2** | **цвет**: кислотный жёлтый, чёрные пятна, сюжет не разобрать (п. 2) |

---

## 1. Кусок вытягивается в палочку на фоне: рябь и затычки на четверти панно

**Проблема.** На спокойном фоне откус растёт вдвое (`MaxAlongScale = 2.0`), а поперечник
прибит к толщине пластины, поэтому кусок выходит 20×7 — палочка 3:1. Жёсткая палочка не
ложится вдоль изогнутого курса: на каждом повороте между соседями остаётся клин, клин
уходит в слияние, на его месте появляется затычка (кусок без курса). Затычек 15–32 % панно,
и они же самые многоугольные куски. Глазом это рябь в фоне.

**Замысел верен, реализация — нет.** Мозаичист кладёт фон крупнее, но крупнее **в обе
стороны**. Проверено единым откусом 20 мм по всему панно, без всякого разнобоя между
курсами: затычек 36–49 %, четырёхугольников 22–30 %, игл 4,6–7,7 %, голое пятно > 0,3
модуля 21–24 % — хуже базы. Виновата пропорция куска, а не смешение размеров.

**Чем подтверждается — экспериментом** (`MaxAlongScale` 2,0 → 1,0, среднее по 24 прогонам):
затычки 23,6 → **10,8 %**, четырёхугольников 35,5 → **47,0 %**, семиугольников и выше
3,2 → 1,2 %, иглы −58 %, излом −16 %, площадь широких зазоров −20 %. Цена: кусков +3,6 %,
ΔE +1,3 % (у human 30×30 8,5 → 10,1). На 1,5 — примерно половина выигрыша.
**На картоне** (gull 30×30, gull-2 15×15): фон перестаёт рябить и складывается в дуги
вокруг силуэта. Ни один из восьми кадров не стал хуже.

**Что делать.** Не убирать удлинение, а ограничить **пропорцию** куска — не длиннее ~1,4
поперечника. При нынешней смальте (7 мм) это фактически базовый откус; при более толстой
пластине правило само разрешит фон покрупнее, честно, в обе стороны.

**Где в коде.** `Core/Grid/Tessellation.cs`: `MaxAlongScale`, `ResizedAlong`.

---

## 2. Цвет уезжает от фотографии: панно по светлоте, отдельные куски по тону

Два механизма одного дефекта — держим вместе, потому что второй порождается первым и
чинить их врозь бессмысленно: подняв светлоту, ближайшим к тёмно-зелёному оказывается
светлый жёлто-зелёный, а после свёртки до 12 цветов — жёлтый. На landscape-2 из-за этого
исчезли гора и небо, а лес ушёл в оливково-жёлтую кашу: **сюжет не читается вовсе**.

### 2а. Панно систематически уезжает по светлоте: до ±15 L\*

Картон целиком переэкспонирован или недоэкспонирован относительно фото, причём **в разные
стороны у разных снимков**, так что скомпенсировать это при съёмке нельзя.

Медиана (L\* положенного артикула − L\* снятой ячейки), 15×15 / 30×30:

| фото | ΔL med | | фото | ΔL med |
|---|---|---|---|---|
| landscape-2 | **+16,0 / +13,4** | | dolphin | +4,5 / +4,9 |
| dolphin-2 | +11,0 / +10,8 | | gull | +1,0 / +1,2 |
| landscape | +9,1 / +9,8 | | gull-3 | −10,7 / −9,6 |
| human | +8,9 / +4,1 | | gull-2 | **−15,5 / −14,6** |

Разброс между фото — 31 единица L\*. Это не погрешность подбора, а разная экспозиция.

**Где в коде.** `Core/Quantization/ToneMap.cs`, `IntoPaletteRange`: диапазон фото [p2, p98]
линейно натягивается на диапазон всей палитры по концам. Середина ни к чему не привязана,
поэтому медиана уезжает ровно на смещение середины фото относительно середины палитры.

**Осторожно.** Растяжка нужна (без неё чайка — 71 % панно одним артикулом), убирать нельзя.
Надо привязать середину.

### 2б. Улёт по тону: 5–10 % цветных кусков уходят на 40°+

На landscape-2 лежит кислотно-жёлтый, которого в исходнике нет вообще. Это не «приблизился
как смог», а смена тона — на панно она будет кричать.

Доля кусков, у которых и фото, и артикул хроматичны (C\* > 8), а угол тона в Lab разошёлся
больше чем на 40°:
- landscape — **9,9 % / 8,4 %** (15×15 / 30×30), p95 расхождения **45,9° / 47,5°**
- landscape-2 — **8,0 % / 5,5 %**, p95 **67,2° / 49,8°**
- dolphin-2 — 7,5 % / 3,4 %, dolphin — 5,0 % / 1,8 %
- у чаек и human — ≤ 0,8 %

Одновременно медиана хроматичности падает (ΔC med −6,8…+2,3): цвет и глохнет, и уводится.

**Где в коде.** `Core/Quantization/Quantizer.cs` (`Map` / `NearestIndex`) на евклидовой
дистанции в Lab (`Colors/ColorDistance.cs`, Cie76). Евклид одинаково оценивает потерю
насыщенности и смену тона, а глаз — нет: тон заметнее. Нужен явный штраф за смену тона.

**Мерить вместе.** Починив только 2а, можно не увидеть улучшения на картоне: тон всё равно
уводит. Оба замера (`dL_med`, `hueDrift`) снимать одним прогоном.

---

## 3. Крошево там, где нет структуры: внутри силуэта и в плоском фоне

**Проблема.** Курсы строятся только там, где рядом есть контур или текстура фотографии. Где
их нет — куски ложатся вразнобой, «сумасшедшей кладкой». На картоне это видно как:
- **грудь чайки** (gull, gull-2) — горсть риса вместо рядов, курсы внутри силуэта не идут;
- **небо landscape** — половина панно мелким крошевом;
- **лицо human** — не читается: деталь мельче куска, а рядов нет.

**Чем подтверждается** (`--why`, восемь фото):
- курсы с базовым откусом идут в среднем **2,8 модуля** — до трёх кусков;
- **30–76 % принятых линий тока упираются в структурные барьеры** (бордюр, контурные ряды),
  то есть заливка не может отойти от них и разойтись по полю;
- попытка вырастить курс в пустом месте (`Trace` в дыру) удаётся в **9–31 %** случаев.

**Проверено и отвергнуто.** Снятие `if (pass == 0)` в шаге 5 (растить курс в дыре на всех
проходах, а не только на первом): затычки 28 → 26 %, остальное не двинулось. Дыра шириной
0,3 модуля не даёт линии пройти минимальную длину — независимо от прохода.

**Мерить не так.** Метрика «курсов < 3 кусков» дефектом не является: ряд той же длины в
миллиметрах, просто набранный крупнее, — нормальный приём. Мерить долю затычек, форму куска
и смотреть картон.

**Где в коде.** `Core/Grid/Tessellation.cs`: посев (`SeedAlong`, `Drain`, подметающая сетка в
`Place`) и барьеры структурных курсов (`RegisterBarrier`). Требует своей диагностики —
делать после пункта 1 и заново мерить: часть может уйти вместе с ним.

---

## 4. Форма куска: четырёхугольников только треть, остальное — пяти- и шестиугольники

**Проблема.** Смальту берут кусачками или молотком: скол идёт сквозь пластину, кусок выходит
четырёхугольным. Пятиугольник со скруглённым углом руками не откусывается — новичок будет
либо подгонять полчаса на кусок, либо класть не то, что нарисовано, и раскладка поедет.

**Чем подтверждается** (до лечения пункта 1): 4 угла — **33–44 %**, 5 углов — 37–42 %,
6 углов — 17–26 %, 7 углов — 1,2–5,4 %. Вогнутых кусков 0 %, самопересекающихся 0 %,
минимальный внутренний угол p1 = 44–55° — куски выпуклые и не иглы, просто многоугольные.
«Стрелки» и «галочки» на картоне — это не куски, а дыры в шве (пункт 6).

**Наполовину закрывается пунктом 1.** Четырёхугольников по роли куска: структурный курс —
медиана 51 %, торец курса заливки — 31 %, середина — 32 %, **затычка — 25 %** (до 5,51
вершины в среднем). Эксперимент по пункту 1 поднимает долю с 35,5 до 47,0 % без единой
правки в резке. Потолок «через пункт 1» — около 51 %: столько даже у прямых параллельных
структурных курсов. Вторую половину брать здесь.

**Где в коде.** `Core/Grid/Tessellation.cs`, `CutCells` — построение полигона тессеры между
соседними линиями тока. Ограничить до четырёхугольника (при необходимости — прямым
отрезанием лишних вершин), либо доказать, что 5–6 углов режется, и снять вопрос.

---

## 5. Крап: до 6 % одиночных кусков и до 13,5 % кусков в островах по 1–2 штуки

**Проблема.** На картоне landscape и human посреди ровного неба лежат одиночные белые и
чёрные куски. В стекле белый — самый громкий артикул, одна такая тессера в сером небе
читается как ошибка набора, а не как оттенок.

**Чем подтверждается.** Доля тессер, чей артикул не совпал ни с одним соседом (и из них —
«громкие», > 15 ΔE от среднего по соседям), плюс доля кусков в островах одного артикула
размером 1–2:

| фото / размер | одиночных | из них громких | в островах 1–2 |
|---|---|---|---|
| landscape 15×15 | **6,1 %** | **5,6 %** | 8,9 % |
| human 15×15 | 5,6 % | 4,4 % | **13,5 %** |
| landscape-2 15×15 | 5,1 % | 4,4 % | 12,4 % |
| gull-3 15×15 | 3,1 % | 1,2 % | 5,5 % |
| gull 30×30 | 0,5 % | 0,4 % | 1,5 % |

На панно 15×15 из 213 кусков 5,6 % громких одиночек — это 12 кусков, которые видно все.
Хуже всего на **15×15**, а это рабочий размер номер один.

**Где в коде.** `Core/Quantization/CoherentMap.cs` (`Settle`) вызывается дважды и должен это
гасить, но не гасит. Смотреть силу связи с соседями и то, что считает соседом
`CellNeighbourhood`: у затычки круг соседей другой, а затычек сейчас четверть панно (п. 1).

---

## 6. Дыры в раскладке до 6 мм при номинальном шве 1 мм

**Проблема.** Именно эти дыры глаз читает как «стрелки» и «галочки» на картоне (куски тут ни
при чём, см. пункт 4). Набирая поверх картона, пользователь либо оставит дыру, либо начнёт
двигать соседей и уедет от раскладки.

**Чем подтверждается.** Ширина шва по площади шва (удвоенное расстояние до ближайшей
тессеры) при номинале 1,0 мм: p50 = **0,8 мм**, p90 = 1,6–2,0 мм, максимум **4,9–6,5 мм**;
площадь с зазором > 3 мм — 0,3–0,8 % панно; площадь шва целиком 24–25 % при номинальных
20,5 % для 10×7 мм со швом 1 мм.

**С пунктом 1 связано слабо — проверено.** Гипотеза «дыра там, где курс оборвался» не
подтвердилась: концентрация зазоров > 3 мм у торцов курсов ×1,3 (медиана), у затычек ×0,9 —
те их скорее закрывают, чем создают. Пункт 1 уменьшает площадь широких зазоров лишь на 20 %.

**Где в коде.** `Core/Grid/Tessellation.cs`, `CutCells` — дыра там, где соседние курсы
расходятся, а резка ячеек оставляет между ними клин.

---

## 7. Один артикул забирает до 59 % панно, нижняя половина артикулов — 10–12 % на всех

**Проблема.** Ровный фон, набранный одним артикулом на пол-панно, в стекле мёртв: смальту для
того и кладут курсами в 2–3 близких оттенках, чтобы поверхность играла. Сейчас бюджет в
12 цветов уходит на три артикула, а девять делят остаток.

**Чем подтверждается.**

| фото / размер | самый частый артикул | топ-3 | нижняя половина артикулов вместе |
|---|---|---|---|
| gull-2 A4 | **59,4 %** | 77,9 % | 10,2 % |
| gull-2 30×30 | 55,4 % | 70,8 % | 12,3 % |
| gull 15×15 | 44,9 % | 78,5 % | 12,1 % |
| landscape-2 30×30 | 19,3 % | 42,1 % | 32,8 % |

На gull-2 самое большое связное пятно одного артикула — **55,4 % панно**.

**Где в коде.** Свёртка (`Core/Quantization/PaletteReducer.cs`) скорее не виновата: она роняет
оттенок по стоимости ошибки, а ровное небо ронять некуда — там уже один оттенок. Смотреть
выше — `ToneMap` (пункт 2а) растягивает глобально и не может разделить то, чего не разделила
фотография. Нужен явный ход мозаичиста: делить крупное ровное поле на 2–3 соседних артикула
по курсам, а не по шуму.

**Прежде чем ставить цель** — померить дно: на 12 оттенках какая-то доминанта неизбежна.

---

## 8. Бордюр съедает 18–20 % кусков на 15×15 и рвётся по краям

**Проблема.** На самом ходовом размере пятая часть всей работы уходит в декоративную рамку.
Плюс на dolphin и dolphin-2 бордюр местами отсутствует: куски выходят к самому краю поля и
упираются в широкий зазор.

**Чем подтверждается.** «Кусков в бордюре»: 15×15 — **17,8–20,4 %**; A4 — 11,9–12,6 %;
30×30 — 9,8–10,4 %. Разрывы по краю видны глазом на `dolphin/15x15`, `dolphin-2/15x15`.

**Где в коде.** `Core/Grid/Tessellation.cs`, `BorderCourseCount` и укладка колец (`RectRing`).
Проверить, от чего считается доля поля и почему на части фото кольцо не замыкается.

---

## 9. Картон печатается без легенды, кодов, контура панно и масштабной линейки

**Проблема.** Пользователь определяет артикул на глаз по цвету картона и сверяется с экраном.
Это работает, пока 10–15 артикулов различимы **на печати**, а сейчас ничто этого не
гарантирует: свёртка выбирает оттенки по стоимости ошибки, и два близких серых легко проходят
вместе. Плюс на листе нет ни контура панно, ни линейки, чтобы проверить масштаб печати.

**Что делать.**
1. **Полоса легенды на самом картоне**, за пределами поля мозаики: плашка артикула, его номер
   и число кусков. Тогда картон самодостаточен.
2. **Контрольная линейка 100 мм и контур панно** — сразу видно, если принтер отмасштабировал.
3. **Гарантия различимости**: не выпускать в набор две смальты ближе некоторого ΔE; если
   свёртка их оставила — слить либо предупредить в интерфейсе.
4. Коды в каждой тессере не нужны: при 10–15 цветах они забьют лист.

**Где в коде.** `Core/Skia/SkiaMosaicRenderer.RenderCartoon`, `Core/Rendering/RenderOptions.Cartoon`,
`Views/Mosaic/Result.cshtml`.

---

## 10. Куски уже 5 мм: p1 узкой стороны 4,0–4,5 мм

**Проблема.** Замерами с натуры порог узкой стороны — 5 мм: уже этого кусок не поставить.
Сейчас 1 % кусков за порогом, 5 % — на самой границе.

**Чем подтверждается.** Ширина куска (минимум по направлениям, а не по осям курса):
p1 = **4,03–4,48 мм**, p5 = 4,43–4,87 мм. Стенд по своей мерке (`minSideP5` вдоль оси курса)
даёт 4,7–5,2 мм. Расхождение между двумя мерками само по себе стоит свести.

**Где в коде.** `Core/Grid/Tessellation.cs` — отсечение слишком узких кусков;
`tools/MosaicGenerator.Diag/Metrics.cs` — `Extent` меряет вдоль оси курса, а не по кратчайшей
ширине полигона.

---

## 11. Дефолты формы не совпадают с рабочим режимом

Форма открывается на 8 цветах и откусе 6 мм. Рабочий режим — 10–15 цветов и откус 10–12 мм.
Каждый прогон начинается с исправления двух полей.

**Где в коде.** `Web/Models/GenerateFormModel.cs`: `MaxColors = 8`, `ModuleAlongMm = 6`.
Панно по умолчанию 15×15 — верно, менять не надо.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 30×30 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Печать больше A4 пока не планируется, но склейка понадобится: разрезка на
листы с перекрытием, метками совмещения и номером листа; лучше PDF, чтобы масштаб не зависел
от просмотрщика.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 30×30 см →
3600 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.

---

## 13. Мелочи и техдолг

- **`RenderGeometry.Clamp` — мёртвый код.** Прижимает вершины тессеры к границе поля, но
  замерено: 0 % кусков выходит за поле на всех восьми фото. Либо убрать, либо превратить в
  проверку с падением. `Core/Rendering/RenderGeometry.cs:129–141`.
- **Предупреждение сборки CS8604** — возможный null в `sourceId`,
  `Web/Controllers/MosaicController.cs:120`. Единственное предупреждение в сборке.
- **Устаревшая подпись под результатом.** `Views/Mosaic/Result.cshtml`: «набранное панно сядет
  чуть глуше картона». Компенсацию шва выпилили, а шов рисуется прямо на картоне — утверждение
  перепроверить и переписать или убрать.

---

## Решено, не всплывать снова

- **Обрыв линии тока по расстоянию — не дефект**, а штатный конец по Jobard–Lefèvre (99 % всех
  обрывов). `dTest` и `dSeed` не трогать.
- **Схема с номерами**: коды в схеме — индексы легенды, не артикулы. Схема нужна только для
  разбора алгоритма, это не дефект.
- **CIEDE2000** замерялся, на палитре смальты не помог; код-заглушки спят под Cie76.
