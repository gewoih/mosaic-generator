### 1. Реалистичное превью → плоский картон — сделано 2026-09-04

[`docs/karton-vmesto-preview-plan.md`](docs/karton-vmesto-preview-plan.md). Пункт переформулирован
в обсуждении: не третий режим рядом с превью, а **замена превью картоном**. Набор идёт прямо
поверх распечатанного картона (уточнено у мастера).

Реалистичный рендер удалён целиком — поля `ToneJitter`/`GlossJitter`/`RotationJitterDeg`/
`SizeJitter`/`EdgeRoughness` и мёртвый `BrickBond` из `RenderOptions`, поворот и «чип» в
`RenderGeometry.Shape`, градиент блика в `FaceColours`, `GlossLow/High` из `RenderedModule`,
шейдер в `SkiaMosaicRenderer`, класс `DeterministicRandom`. `RenderOptions.Preview` →
`RenderOptions.Cartoon` (плоская заливка цветом артикула, шов, `PixelsPerStep = 96`) сквозь
ядро, веб (`ResultImage.Cartoon`, роут `result/{id}/cartoon.png`, `CartoonPixelsPerStep`) и
стенд. **Схема с номерами оставлена** — только для разбора раскладки.

Проверено: `dotnet test` 254/254; стенд 24 прогона (8 фото × 15×15/A4/30×30, откус 6 мм) без
падений; 30×30 выходит ~4114 px (≈348 dpi при печати 1:1) против прежних 87 dpi. Юнит-тест
подтверждает: внутри одной цветовой зоны цвет строго один и точно равен артикулу.

Осталось хвостом: границы зон и код артикула поверх картона — пункт 3; читаемость схемы —
пункт 17; разбивка на листы, кресты, линейка, легенда — пункт 4. Аргументация (ΔE соседних
артикулов, 17 раскладов из 24 с парой ближе 8 ΔE) — в плане.

---

### 2. Цветной ободок по краю фигуры (розовая полоса на спине дельфина)

**Что не так.** Внешний ряд силуэта (`+dAcross/2` от контура) сидит вплотную к границе фигуры, и
`CellSampler` усредняет по полигону куска полосу, задевающую край. На `dolphin/30x30-m6` вдоль всей
спины идёт розоватый ряд, которого нет ни в воде, ни в животном.

**Почему это важнее, чем выглядит.** Прямой набор: мастер положит этот ряд буквально. Единственная
прямая ошибка цвета во всей выборке.

**Что сделать.** Либо отодвинуть внешний ряд на `+3·dAcross/4`, либо в `CellSampler.AveragePolygon`
для куска контурного курса брать цвет большей половины (2-means уже есть), а не среднее.

**Как проверить.** Восемь фото: ободок ушёл на `dolphin`, остальные семь картонов не сдвинулись
(или сдвинулись в сторону меньшего ΔE).

**Где.** `Core/Grid/CellSampler.cs`, `Tessellation` шаг 2 (`OutwardOffsets`).

---

### 3. На картоне нет границ зон и нет артикула

**Что не так.** Плоский картон (п. 1 сделан) — картинка, а не карта. Границу цветовой зоны видно
только по перепаду цвета — а при парах в 3–7 ΔE (см. план п. 1) её не видно вовсе. Какой артикул
класть, картон не сообщает: цвет есть, имени нет, а купленная плитка лежит по артикулам.
**Набор идёт прямо поверх картона** — центр зоны закроется первым же куском, значит границы и
код нужны по краю зоны, не в центре.

**Что сделать.**
- Тонкая линия по границе цветовой зоны (там, где соседние куски разного артикула), поверх заливки.
- Код артикула **один на зону**, а не в каждом куске.
- Предупреждение в форме и в ведомости, когда в панно попала пара ближе ~5 ΔE: различить их на
  печати нельзя, значит либо помечать штриховкой, либо принимать сознательно.

**Как проверить.** На восьми фото по картону читается, где кончается зона, при закрытой половине
панно уже уложенными кусками.

**Где.** новый режим рендера из п. 1; `Core/Material/MaterialReport.cs` (коды уже есть).

---

### 4. Картон не выходит из принтера

Три известные дыры, лечатся одним куском работы — без них картон физически не использовать.

- **Панно A4 не влезает на лист A4.** Схема печатается ровно в размер панно, 210×297 мм, масштаб
  1:1 верный, но поля принтера этого не позволяют. Подсказывать в форме: либо панно чуть меньше
  листа (19×27 см), либо A3.
- **Нет разбивки на листы и меток совмещения.** 30×30 при 6 мм — 2057 px, четыре листа A4 встык,
  крестов нет.
- **Нет масштабной линейки.** Печать 1:1 заявлена через dpi в PNG, проверить нечем, а принтер врёт
  масштаб регулярно. Хватит 10-сантиметровой линейки на поле.
- **Нет легенды на самом листе:** артикул / образец цвета / число кусков / площадь с запасом.
  Сейчас это только веб-страница.
- **Вектор вместо PNG.** Для печати 1:1, тем более на A3 или плоттере, PDF/SVG надёжнее: не зависит
  от dpi и не мылит линии. Заодно снимает вопрос разрешения из п. 1.

Зеркальный вывод **не нужен** — набор прямой (уточнено у мастера 2026-09-03).

**Где.** `Core/Skia/SkiaMosaicRenderer.cs`, `Web/Controllers/MosaicController.cs`,
`Web/Views/Mosaic/_Parameters.cshtml`, `Result.cshtml`.

---

### 5. Клинья (филлеры) на длинном откусе

**Что не так.** Кусок, не принадлежащий никакому курсу, — клин между двумя не сошедшимися линиями
тока. Доля таких кусков:

| фото | 6 мм (15/A4/30) | 10 мм (15/A4/30) |
|---|---|---|
| gull | 10,9 / 8,9 / 8,7 | **27,3 / 28,5 / 29,7** |
| gull-2 | 11,1 / 10,4 / 7,9 | **32,2 / 29,4 / 31,4** |
| gull-3 | 7,1 / 6,0 / 5,8 | 14,5 / 18,5 / 19,3 |
| dolphin | 8,2 / 7,8 / 4,5 | 18,3 / 17,4 / 16,9 |
| dolphin-2 | 8,3 / 6,2 / 6,9 | **24,9 / 29,4** / 18,9 |
| human | 5,3 / 7,7 / 7,5 | 16,7 / 12,5 / 13,4 |
| landscape | 4,8 / 7,6 / 5,5 | 16,4 / 17,3 / 20,1 |
| landscape-2 | **23,1** / 15,2 / 15,9 | 22,6 / 15,7 / 13,8 |

**Прежний диагноз был неверен.** Формулировка «филлеры растут с размером панно» построена на
замерах только 6 мм. При 6 мм с ростом панно клиньев обычно **меньше**; настоящий драйвер —
**длина откуса**. При 10 мм (середина вашего рабочего диапазона 6–12 мм) на чайках клин — каждый
третий кусок. Заодно четырёхсторонних кусков остаётся 31–38 % против 56–60 % при 6 мм.
`landscape-2` — единственное исключение, ему плохо на любом откусе (см. п. 11).

**Что уже пробовали и что не работает.** Priority-вариант Jobard–Lefèvre (трассировать пачку семян, класть длиннейшего, проигравших назад
в очередь) замерен по восьми фото и четырём размерам — картон не сдвинулся, обрубки на 15×15 стало
хуже (28,8 → 30,5 %), филлеры не изменились, время 60×60 ×2,4. Семена заливки — смещения уже
уложенных курсов, в пачке почти-дубликаты, выбор длиннейшего ничего не меняет. Клинья идут из
геометрии поля, а не из порядка очереди.

**Оставшиеся пути.** Разрезка Вороного зажатой ячейки (раньше решили не трогать — но замер по
10 мм меняет цену вопроса); либо поле направлений в зонах схождения эха (пересекается с п. 8);
либо честно ограничить выбор в форме 6–8 мм, пока не починено.

Не трогать `dTest`: порог расстояния уже опускали, дальше он пакует курсы теснее куска.

**Как проверить.** Доля клиньев на 10 мм не выше, чем на 6 мм. Медиану длины курса под куском
(стенд её печатает) не ухудшать.

**Побочно.** Комментарий в `Tessellation.cs` про «две трети обрывов — расстояние, треть — кривизна»
устарел: счётчик `Tessellation.BreakReasons` по восьми фото даёт расстояние 95–100 %, кривизну
0–1 %. Поправить при следующем касании файла.

**Где.** `Tessellation.StreamlinePlacer.Drain` / `Place`, `minLength`.

---

### 6. Одиночные грязные куски (крап)

**Что не так.** В ровном фоне сидят одиночные куски чужого артикула. На `gull-3/30x30-m6` — почти
чёрные куски посреди синего неба, на `dolphin-2/30x30-m6` — тёмные точки по светлому телу. Глазами
видно сразу, в цифре прячется. При прямом наборе мастер вставит чёрный кусок в середину неба.

**Диагноз (пересмотрен, остаётся в силе).** Это **не** дефект `CoherentMap`. При выключенном
соседском члене `merged` = 0,390 — то есть бо́льшая часть даёт сама арифметика двенадцати оттенков
(шаг тона 7,1 ΔE, любая пара внутри полосы обязана слиться). На согласование с соседями приходится
0,048. Три формулировки замерены (плана в `docs/` не осталось, цифры только здесь): лучшая даёт `merged` 0,373 при `banding` 0,050 против 0,438 / 0,043, но
**структурно не может убрать одиночный кусок** — поштучный подбор переводит ячейку на другой
оттенок только если она отошла от соседей больше чем на половину расстояния между оттенками,
значит крап всегда приходит с готовым разрывом.

**Вывод.** Любое правило, где цель для пары считается по разрыву **этой же пары**, различить крап
и градацию не может в принципе. Крап опознаётся пространственно — одна ячейка против многих.
Нужен **второй член** поверх существующего: агрегирующий штраф за несогласие остаётся, к нему
добавляется поправка по согласованному градиенту окрестности.

**Как проверить.** Отдельная метрика «изолированный кусок»: доля кусков, отличающихся от **всех**
соседей больше чем на шаг тона. Сейчас стенд её не считает — добавить, замерить дно, потом ставить
цель. `merged` по восьми фото 0,27–0,54, и он этого не ловит.

---

### 7. Каждый двадцатый кусок ниже порога откуса

**Что не так.** `узкая сторона p5` = **4,5–5,2 мм** во всех 48 прогонах при порогe узкой стороны
5 мм (замер с натуры). То есть ~5 % кусков кусачками не выкусить — их придётся либо выбрасывать,
либо переигрывать соседей по месту.

Это не свойство сюжета и не свойство размера: цифра одинакова на всех восьми фото, всех размерах
и обоих откусах. Значит она идёт из самой раскладки — ячейка Вороного зажимается там, где сходятся
курсы.

**Что сделать.** Не решено. Либо слияние слишком узкой ячейки с соседом на шаге 5 `Tessellation`,
либо сдвиг границы Вороного до порога. Замерить, насколько это ухудшит покрытие и `дыру` (п. 9).

**Где.** `Tessellation.Advected` шаг 5, `Metrics.Shape.MinSideP5`.

---

### 8. Флаг `IsCut` означает не то

**Что не так.** Флаг стоит у **69–85 %** кусков (растёт с панно). При раскладке диаграммой Вороного
режется практически каждая ячейка, поэтому счётчик резов, задуманный для сметы, считает почти всё
панно и ничего не сообщает.

**Что сделать.** Сменить смысл: не «был обрезан», а «отличается от номинального прямоугольника
больше чем на столько-то». Тогда счётчик покажет, сколько кусков придётся подгонять руками, — а это
и есть то, что мастер хочет знать перед работой. Пересекается с п. 7.

**Где.** `Tessellation.Finish`, `Tessera.IsCut`, `MosaicResult.CutTesseraCount`.

---

### 9. Щель на концах курсов — растёт с размером панно

**Что не так.** Наибольшая пустая окружность там, где сходятся концы курсов и углы колец бордюра:

| панно | среднее | максимум |
|---|---|---|
| 15×15 | 0,409 | 0,429 |
| 30×30 | 0,462 | 0,547 |
| 60×60 | 0,513 | 0,714 |
| 90×60 | 0,531 | 0,714 |

До 0,71 модуля — почти целый недостающий кусок. Суммарная голая площадь мизерная (0,1–0,3 %), но
пятно в один кусок глаз находит.

**Связь с п. 5.** Причина та же — концы курсов, не дошедших друг до друга. Перемерить после п. 5;
после непрерывной обводки силуэта `дыра` на dolphin 60×60 уже сдвинулась 0,24 → 0,40.

**Где.** `Tessellation.Advected` шаг 5: ячейка Вороного не может вырасти за пределы стартового
прямоугольника, поэтому там, где соседа нет вовсе, она не дотягивается.

---

### 10. Метрика `rare` не ловит артикул в четыре куска

Минимальное число кусков на артикул: `human` 15×15 — **4 шт**, `gull` 15×15 — 5, `gull-2` 15×15 — 5,
`dolphin-2` 15×15 — 5, `gull` 30×30 — 5. Ради них покупается целая плитка. Метрика `rare` при этом
показывает **0** на всех восьми фото и всех размерах — её порог такие случаи не видит вообще.

Починить порог (доля от панно не работает на маленьких панно — считать в штуках), и показывать
такие строки в ведомости отдельно: «5 кусков — возможно, шум, стоит свернуть».

**Где.** `Metrics.Colours` (`RareColors`), `Web/Views/Mosaic/Result.cshtml`.

---

### 11. Нет предупреждения о неподходящем сюжете

`landscape-2` выпадает по всем метрикам: филлер 13,8–23,1 % при любом откусе, излом 18,8–34,7 %
(максимум по выборке), ΔE 17,5–19,7 против 5–13 у остальных. Глазами — каша: густая листва, нет
ни фигуры, ни силуэта, полю направлений цепляться не за что.

Это не дефект алгоритма, это неподходящее фото — так и должно быть. Дефект в том, что **форма
принимает такое фото молча** и выдаёт картон, по которому честно наберётся каша. Стенд разницу
видит ясно; надо показать её мастеру до генерации.

**Как проверить.** Семь фото проходят без предупреждения, `landscape-2` — с предупреждением.

---

### 12. Бюджет оттенков по важности, а не по площади

Один оттенок занимает до **62 %** панно (`gull-2` 30×30 — 0,62; `gull` 15×15 — 0,61). Фон забирает
площадь и вместе с ней бо́льшую часть из 12 оттенков, фигуре не остаётся. Побочно это и вопрос
закупки: одна плитка кончится посреди работы.

**Что сделать.** Взвесить `PaletteReducer` по важности ячейки, а не только по частоте. Вес брать из
уже посчитанного — `DistanceField` (близость к контуру) и `field.EdgeAt`. Ровное небо получает вес
ниже единицы, зона у силуэта — выше.

**Как проверить.** Множитель ΔE (`dE_mean` к медиане охвата палитры) не выше 1,4 на шести фото
(`landscape-2` и `dolphin` исключить — у них 42–43 % блоков дальше 10 ΔE, это предел материала).
`dominant` ниже 0,45. Обязательное условие: `gull` не должен ухудшиться, он сейчас на 1,20, и суть
в том, чтобы остальные подтянулись к нему, а не он опустился к ним.

---

### 13. Прямой курс по сильной горизонтали

На фотографии дельфина резкая линия горизонта на 40 % высоты. В эскизе её нет: фоновые курсы
обходят дельфина дугами и пересекают горизонт как ни в чём не бывало.

Сильная прямая в сюжете — это **структура**, а не край. Мозаичист кладёт такую линию отдельным
прямым курсом, и именно он держит композицию. `ContourSet` эту линию находит, но `Tessellation`
не даёт ей приоритета над эхом силуэта.

Приоритет низкий: на пейзаже, где горизонталей больше всего, линия воды и кромка ельника
отработали приемлемо.

---

### 14. Поле направлений почти не ведётся фотографией

Стенд печатает: «поле ведёт фотография на **1–2 %** панно; эхо уводит остальное на 18–24°».
Когерентность тензора p90 = 0,10, p99 = 0,27 при пороге доверия 0,35 — фотография практически
никогда его не проходит. То есть 98 % направления курсов задаётся расстоянием от силуэта, а не
формой объекта.

На чайках это сходит за opus musivum и смотрится хорошо. На `human` — хуже: курсы вокруг фигуры
сворачиваются в лабиринт, тело не лепится.

Порог 0,35 проверялся по метрикам гладкости и признан оптимальным. **Но с этой стороны — «сколько
панно вообще ведёт фотография» — он не мерился.** Перемерить именно так, прежде чем что-то менять.

---

### 15. `MaxAlongScale = 2.0` в `Tessellation.ResizedAlong`

Множитель, на который курс по спокойному фону длиннее курса у силуэта. Комментарий в коде
обосновывает его как «6–8 мм база → 12–16 мм фон, размер настоящей мозаики» — но это не перемерено
по восьми фото. При базе 6 мм потолок фона 12 мм, что может быть мало относительно традиционной
укладки (там фон кладут в 2–3 раза крупнее детали), но панно здесь 15–30 см, а не монументальная
мозаика: кусок длиннее ~2–2,5 баз в одном курсе рискует читаться «палочкой», а не тессерой.

Замерить выборку значений (2.0 / 2.5 / 3.0) по метрикам курса / filler / kink. Стенд уже печатает
нужное: «длина курса, кусков: p10/p50/p90/max», «курс под случайным куском: p25/p50/p75».

---

### 16. Обрубки: курсы короче трёх кусков

Доля курсов короче трёх кусков — **15,6–51,4 %** по восьми фото (растёт с длиной откуса). Несут они
немного, 9–33 % кусков, но `minLength = 2.0 * dAlong` и второй `Drain` с `minLength * 0.5` прямо
разрешают курс в один-два куска.

Отбор кандидатов по длине пробовали — не помогло (см. п. 5). Цель: ниже 15 %.

---

### 17. Редкие коды на схеме

Схема с номерами — инструмент разбора алгоритма, не картон (уточнено 2026-09-03). Но код печатается
в каждом куске, и лист нечитаем: на `human/30x30-m10` полторы тысячи цифр, изображения не видно.
Один код на цветовую зону сделает схему пригодной хотя бы для разбора. Пересекается с п. 3, где то
же самое нужно на картоне.

**Где.** `SkiaMosaicRenderer.RenderScheme`.

---

### 18. Хвосты закрытых пунктов

Закрытые задачи и их аргументация — в `docs/`:
[непрерывная обводка](docs/nepreryvnaya-obvodka-plan.md),
[бордюр по размеру](docs/bordyur-po-razmeru-plan.md),
[выбор откуса](docs/vybor-otkusa-plan.md).
Планы по жадному посеву и по цвету курсов до `docs/` не доехали — их выводы перенесены
в пп. 5 и 6 этого файла.
Здесь — только то, что осталось несделанным.

- **`Diag/Program.cs`: `structural = borderRings + cs.Count * 2` завышает контурные курсы на
  маленьких панно** — силуэт там кладётся одним рядом, не двумя. Метрика `inBorder` (от
  `BorderCourseCount`) верна, врёт только сплит «контурные / заливка». Считать по фактическому
  числу структурных курсов.
- **Второй ряд силуэта строго снаружи — сверить с бордюром** (общая зона, `RectRing` и
  `OutwardOffsets`).
- **15×15 при одном кольце бордюра — 19–24 % кусков в бордюре.** Уронить к ~14 % можно, только дав
  одиночному кольцу длинный откус на спокойных участках, но тогда кольцо перестаёт быть регулярным
  и меняет характер бордюра. Браться, если 15×15 на глазах читается тяжело. 24 % — это дно от
  одного кольца, база была 42,6 %.
- **Излом в контурных курсах на 15×15** подрос 11 → 23–28 % после непрерывной обводки: силуэт
  длинный, панно маленькое, куски у стыка курсов зажимаются сильнее. Пересекается с пп. 5 и 7.
- **Прогнать `dotnet test` и стенд после пункта «выбор откуса»** — в той сессии не было .NET SDK,
  проверка была только построчным просмотром и grep'ом. *(Стенд прогнан 2026-09-03, 48 прогонов
  без падений; `dotnet test` — по-прежнему нет.)*

---

### 20. CIEDE2000 замерена, прод не переключён

На палитре смальты CIEDE2000 не помогает; код-заглушки спят под `Cie76`. Стенд умеет
`--metric ciede2000`. Оставлено как есть сознательно.