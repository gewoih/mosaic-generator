# Бэклог

Порядок — по приоритету. Каждый пункт: в чём проблема, чем подтверждается, где в коде.

Все цифры ниже сняты 2026-09-04 стендом `tools/MosaicGenerator.Diag` по восьми фото из
`samples/` на размерах 15×15, A4 (21×29,7) и 30×30 см, откус 10 мм, 12 цветов, плюс
разовыми замерами по тем же прогонам (выпуклость, вершины, ширина шва, одиночные куски,
снос по светлоте/тону) — этих метрик в стенде пока нет, см. пункт 2.

Приоритеты пользователя, заданные явно: **реалистичная геометрия набора, натуральный цвет
(чтобы сюжет читался), реалистичная форма тессеры**. Порядок ниже это отражает.

---

## 1. Курсы рассыпаются: 99 % обрывов — по расстоянию, 15–32 % кусков вообще вне курса

**Проблема.** Курс — основа набора. Сейчас линии тока гибнут не там, где кончается форма,
а там, где не сошлась расстановка, и до трети панно ложится филлерами — одиночными
кусками без курса. Глазом это и есть «крошево» в фонах gull-2 и dolphin-2.

**Чем подтверждается.**
- Стенд, «обрывы курса»: **расстояние 99 %**, край ≤ 1 %, кривизна ≤ 1 %,
  самопересечение 0 %, шаги 0 % — одинаково на всех восьми фото.
- Филлеров: **15–32 %** (gull-2 — 29–32 %, dolphin-2 — 19–29 %, landscape-2 — 14–23 %).
- Длина курса под случайным куском: p25 = **4–11 кусков**, p50 = 7–40. Курс из четырёх
  кусков курсом не читается.
- Доля кусков в заливке (не бордюр, не контурные курсы): **52–81 %**.

**Где в коде.** `Core/Grid/Tessellation.cs` — проверка расстановки в укладчике линий тока
(Jobard–Lefèvre) и `Tessellation.BreakReasons[1]`. Смотреть порог разрежения и что
происходит с куском, когда курс оборван: сейчас он становится филлером, а не продолжением
соседнего курса.

**Эхо от рамки в поле направлений устранено 2026-09-04** (перцентильная нормировка
когерентности, `docs/pole-napravleniy-plan.md`): «поле ведёт фотография» выросло с 1–4 %
до 12–34 % панно. Часть обрывов могла уйти сама — мерить по исправленному полю, база
метрик по нему пересобрана.

---

## 2. Стенд не меряет того, что видно глазом

**Проблема.** По правилам проекта цифры приёмки берутся из `Diag`. Но метрик под четыре
из семи дефектов ниже в стенде нет, и их пришлось считать разовыми скриптами. Без них
пункты 3–7 нечем принять.

**Чего не хватает.**
- **Вершины куска** — распределение по числу углов (пункт 3).
- **Ширина шва** — распределение по площади шва, не средняя (пункт 7).
- **Одиночный кусок** — доля тессер, чей артикул не совпал ни с одним соседом, и из них
  доля «громких» (> 15 ΔE от среднего по соседям) (пункт 5).
- **Острова артикула** — доля кусков в связных пятнах размером 1–2 (пункт 5).
- **Снос по светлоте и тону** — медиана ΔL и доля кусков с уводом тона > 40° между
  снятым с фото цветом и положенным артикулом (пункты 4, 6).
- **Выпуклость и самопересечение полигона** — сейчас 0 % у всех восьми фото, но это
  проверено разово; должно стать регрессией, а не разовой проверкой.

**Где в коде.** `tools/MosaicGenerator.Diag/Metrics.cs` (`Metrics.Shapes`, `Metrics.Colours`)
и шапка CSV в `Program.cs:118–123`.

---

## 3. Форма куска: четырёхугольников только треть, остальное — пяти- и шестиугольники

**Проблема.** Смальту берут кусачками или молотком: скол идёт сквозь пластину, кусок
выходит четырёхугольным. Пятиугольник со скруглённым углом руками не откусывается —
новичок будет либо подгонять полчаса на кусок, либо класть не то, что нарисовано, и
раскладка поедет.

**Чем подтверждается.** Стенд, строка «вершин у куска», по восьми фото:
- 4 угла — **33–44 %**
- 5 углов — **37–42 %**
- 6 углов — **17–26 %**
- 7 углов — 1,2–5,4 %, 8–9 углов — до 0,4 %

Проверено отдельно и **опровергает более страшную гипотезу**: вогнутых кусков **0 %**,
самопересекающихся **0 %**, минимальный внутренний угол p1 = 44–55°, отношение сторон
p99 = 2,8–3,4. То есть куски выпуклые и не иглы — они просто многоугольные.
«Стрелки» и «галочки», которые видно на картоне, — это не куски, а дыры в шве (пункт 7).

**Где в коде.** `Core/Grid/Tessellation.cs` — построение полигона тессеры между соседними
линиями тока. Ограничить до четырёхугольника (при необходимости — с прямым отрезанием
лишних вершин), либо доказать, что 5–6 углов режется, и снять вопрос.

---

## 4. Панно систематически уезжает по светлоте: до ±15 L\* от фотографии

**Проблема.** Картон не просто «немного другой» — он целиком переэкспонирован или
недоэкспонирован относительно фото, причём **в разные стороны у разных снимков**, так что
пользователь не может это скомпенсировать при съёмке или кадрировании. На landscape-2
из-за этого исчезли гора и небо, а тёмный лес ушёл в оливково-жёлтую кашу.

**Чем подтверждается.** Медиана (L\* положенного артикула − L\* снятой ячейки),
15×15 / 30×30:
| фото | ΔL med |
|---|---|
| landscape-2 | **+16,0 / +13,4** |
| dolphin-2 | +11,0 / +10,8 |
| landscape | +9,1 / +9,8 |
| human | +8,9 / +4,1 |
| dolphin | +4,5 / +4,9 |
| gull | +1,0 / +1,2 |
| gull-3 | −10,7 / −9,6 |
| gull-2 | **−15,5 / −14,6** |

Разброс между фото — 31 единица L\*. Это не погрешность подбора, а разная экспозиция.

**Где в коде.** `Core/Quantization/ToneMap.cs`, `IntoPaletteRange`: диапазон фото [p2, p98]
линейно натягивается на диапазон **всей палитры** [p2, p98] по концам. Середина никуда не
привязана, поэтому медиана уезжает ровно на столько, на сколько середина фото смещена
относительно середины палитры. Замысел («материал живёт не в диапазоне камеры») верный —
реализация тянет за концы, не удерживая ключевой тон.

**Осторожно.** Комментарий в файле честно фиксирует, зачем растяжка нужна (чайка: 71 %
панно одним артикулом без неё). Убирать растяжку нельзя — надо привязать середину.

---

## 5. Улёт по тону: 5–10 % цветных кусков уходят на 40°+, включая цвета, которых на фото нет

**Проблема.** На landscape-2 на картоне лежит кислотно-жёлтый, которого в исходнике нет
вообще. Это не «приблизился как смог» — это смена тона, и на панно она будет кричать.

**Чем подтверждается.** Доля кусков с хроматичностью > 8 у обоих (фото и артикул),
у которых угол тона в Lab разошёлся больше чем на 40°:
- landscape — **9,9 % / 8,4 %** (15×15 / 30×30), p95 расхождения тона **45,9° / 47,5°**
- landscape-2 — **8,0 % / 5,5 %**, p95 **67,2° / 49,8°**
- dolphin-2 — 7,5 % / 3,4 %, dolphin — 5,0 % / 1,8 %
- у чаек и human — ≤ 0,8 %

Одновременно медиана хроматичности падает (ΔC med −6,8…+2,3): цвет и глохнет, и уводится.

**Где в коде.** `Core/Quantization/Quantizer.cs` (`Map` / `NearestIndex`) на евклидовой
дистанции в Lab (`Colors/ColorDistance.cs`, метрика Cie76). Евклид в Lab одинаково
оценивает потерю насыщенности и смену тона, а глаз — нет: тон заметнее. Плюс эффект
складывается с пунктом 4 — после подъёма на +16 L\* ближайшим к тёмно-зелёному оказывается
светлый жёлто-зелёный, а после свёртки до 12 цветов — жёлтый.

**Связь с записанным.** CIEDE2000 уже замерялся и на палитре смальты не помог
(пункт про `ciede2000-measured-no-switch`). Речь не о нём: нужен явный штраф за смену
тона, а не другая формула общей близости.

---

## 6. Крап: до 6 % одиночных кусков и до 13,5 % кусков в островах по 1–2 штуки

**Проблема.** На картоне landscape и human посреди ровного неба лежат одиночные белые и
чёрные куски. В стекле белый — самый громкий артикул, одна такая тессера в сером небе
читается как ошибка набора, а не как оттенок.

**Чем подтверждается.** Доля тессер, чей артикул не совпал ни с одним соседом
(и из них — «громкие», > 15 ΔE от среднего по соседям), плюс доля кусков в связных
островах одного артикула размером 1–2:
| фото / размер | одиночных | из них громких | в островах 1–2 |
|---|---|---|---|
| landscape 15×15 | **6,1 %** | **5,6 %** | 8,9 % |
| human 15×15 | 5,6 % | 4,4 % | **13,5 %** |
| landscape-2 15×15 | 5,1 % | 4,4 % | 12,4 % |
| gull-3 15×15 | 3,1 % | 1,2 % | 5,5 % |
| gull 30×30 | 0,5 % | 0,4 % | 1,5 % |

На панно 15×15 из 213 кусков 5,6 % громких одиночек — это 12 кусков, которые видно все.
Хуже всего именно на **15×15**, а это рабочий размер номер один.

**Где в коде.** `Core/Quantization/CoherentMap.cs` (`Settle`) вызывается дважды и должен
это гасить, но по факту не гасит. Смотреть силу связи с соседями и то, что `CellNeighbourhood`
считает соседом при 15–32 % филлеров (пункт 1): у куска вне курса круг соседей другой.

---

## 7. Дыры в раскладке до 6 мм при номинальном шве 1 мм

**Проблема.** Именно эти дыры глаз читает как «стрелки» и «галочки» на картоне (см. пункт 3 —
куски тут ни при чём). Набирая поверх картона, пользователь либо оставит дыру в панно,
либо начнёт двигать соседей и уедет от раскладки.

**Чем подтверждается.** Ширина шва по площади шва (удвоенное расстояние до ближайшей
тессеры), номинал 1,0 мм:
- p50 = **0,8 мм**, p75 = 1,1–1,4 мм — то есть основная масса шва в норме;
- p90 = **1,6–2,0 мм**, максимум **4,5–6,4 мм**;
- площадь с зазором > 3 мм: 0,2–0,5 % панно;
- площадь шва целиком: **21,9–25,4 %** при номинальных 20,5 % для 10×7 мм со швом 1 мм.

Стенд это же видит как `bare03` (голое пятно > 0,3 модуля) = 0,2–3,3 % и «дыра» радиусом
до 0,33 модуля.

**Где в коде.** Тот же укладчик, что и в пункте 1, — дыра появляется там, где курс оборвался
и филлер не встал. Скорее всего, чинится вместе с пунктом 1; проверить, что чинится.

---

## 8. Один артикул забирает до 59 % панно, нижняя половина артикулов — 10–12 % на всех

**Проблема.** Ровный фон, набранный одним артикулом на пол-панно, в стекле мёртв: смальту
для того и кладут курсами и в 2–3 близких оттенках, чтобы поверхность играла. Сейчас
бюджет в 12 цветов уходит на три артикула, а девять делят остаток.

**Чем подтверждается.**
| фото / размер | самый частый артикул | топ-3 | нижняя половина артикулов вместе |
|---|---|---|---|
| gull-2 A4 | **59,4 %** | 77,9 % | 10,2 % |
| gull-2 30×30 | 55,4 % | 70,8 % | 12,3 % |
| gull 15×15 | 44,9 % | 78,5 % | 12,1 % |
| gull 30×30 | 44,2 % | 78,4 % | 11,2 % |
| landscape-2 30×30 | 19,3 % | 42,1 % | 32,8 % |

На gull-2 самое большое связное пятно одного артикула — **55,4 % панно**.

**Где в коде.** Свёртка (`Core/Quantization/PaletteReducer.cs`) тут скорее не виновата: она
роняет оттенок по стоимости ошибки, а не по редкости, и ровное небо ронять некуда — там
уже один оттенок. Смотреть выше: `ToneMap` растягивает глобально и не может разделить то,
что фотография не разделила. Нужен явный ход мозаичиста — делить крупное ровное поле на
2–3 соседних артикула по курсам, а не по шуму.

**Прежде чем ставить цель** — померить дно: на 12 оттенках какая-то доминанта неизбежна.

---

## 9. Бордюр съедает 18–20 % кусков на 15×15 и рвётся по краям

**Проблема.** На самом ходовом размере пятая часть всей работы уходит в декоративную рамку.
Плюс на картонах dolphin и dolphin-2 бордюр местами отсутствует: куски выходят к самому
краю поля и упираются в широкий зазор.

**Чем подтверждается.** Стенд, «кусков в бордюре»: 15×15 — **17,8–20,4 %**;
A4 — 11,9–12,6 %; 30×30 — 9,8–10,4 %. Разрывы по краю видны глазом на
`dolphin/15x15`, `dolphin-2/15x15` (левый и верхний край).

**Где в коде.** `Core/Grid/Tessellation.cs`, `BorderCourseCount` и укладка колец
(`RectRing`, ~строки 168–190). Логика «второе кольцо только если полоса меньше доли поля»
описана в комментарии; проверить, что доля считается от того, от чего надо, и почему на
части фото кольцо не замыкается.

---

## 10. Картон печатается без легенды, кодов, контура панно и масштабной линейки

**Проблема.** Пользователь определяет артикул на глаз по цвету картона и сверяется с
экраном (его слова). Это работает, пока 10–15 выбранных артикулов различимы **на печати**,
а сейчас ничто этого не гарантирует: свёртка выбирает оттенки по стоимости ошибки, и два
близких серых легко проходят вместе. Плюс на листе нет ни контура панно, ни линейки, чтобы
проверить, что принтер не отмасштабировал печать.

**Что предлагается** (это и есть ответ на «может, предложишь вариант получше»):
1. **Полоса легенды на самом картоне** — за пределами поля мозаики: плашка каждого артикула,
   его номер и число кусков. Тогда картон самодостаточен и сверка с экраном не нужна.
2. **Контрольная линейка 100 мм и контур панно** на том же поле — сразу видно, если печать
   ушла в масштабе.
3. **Гарантия различимости**: не выпускать в набор две смальты ближе некоторого ΔE
   друг к другу; если свёртка их оставила — либо слить, либо предупредить в интерфейсе.
4. Коды **в каждой тессере** на картоне не нужны: при 10–15 цветах они забьют лист, а
   пользователь на них не рассчитывает.

**Где в коде.** `Core/Skia/SkiaMosaicRenderer.RenderCartoon` (сейчас рисует только заливки
по цвету шва), `Core/Rendering/RenderOptions.Cartoon`, `Views/Mosaic/Result.cshtml`.

---

## 11. Куски уже 5 мм: p1 узкой стороны 4,0–4,5 мм

**Проблема.** Замерами с натуры порог узкой стороны — 5 мм: уже этого кусок не поставить.
Сейчас 1 % кусков за порогом, 5 % — на самой границе.

**Чем подтверждается.** Ширина куска (минимум по направлениям, а не по осям курса):
p1 = **4,03–4,48 мм**, p5 = 4,43–4,87 мм. Стенд по своей мерке (`minSideP5` вдоль оси
курса) даёт 4,69–5,22 мм и `uncuttable` (< 3 мм) — 0 % везде, кроме landscape-2 A4 (0,13 %).
Расхождение между двумя мерками само по себе стоит свести.

**Где в коде.** `Core/Grid/Tessellation.cs` — отсечение слишком узких кусков;
`tools/MosaicGenerator.Diag/Metrics.cs` — `Extent` меряет вдоль оси курса, а не по кратчайшей
ширине полигона.

---

## 12. Дефолты формы не совпадают с рабочим режимом

**Проблема.** Форма открывается на 8 цветах и откусе 6 мм. Рабочий режим — 10–15 цветов и
откус 10–12 мм. Каждый прогон начинается с исправления двух полей.

**Где в коде.** `Web/Models/GenerateFormModel.cs`: `MaxColors = 8` (строка 58),
`ModuleAlongMm = 6` (строка 33). Панно по умолчанию 15×15 — верно, менять не надо.

---

## 13. Склейка листов A4 с метками (на будущее)

**Проблема.** Картон 30×30 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG
с `pHYs`; резать и склеивать — руками. Пользователь сказал: печать больше A4 пока не
планирует, но склейка понадобится.

**Что нужно, когда дойдёт очередь.** Разрезка на листы A4 с перекрытием, метками совмещения
и номером листа; лучше PDF, чтобы масштаб не зависел от настроек просмотрщика.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб уже пишется корректно:
30×30 см → 3600 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.

---

## 14. Мелочи и техдолг

- **`RenderGeometry.Clamp` — мёртвый код.** Прижимает вершины тессеры к границе поля, но
  замерено: **0 % кусков** выходит за поле на всех восьми фото и обоих размерах. Либо убрать,
  либо превратить в проверку с падением, чтобы не маскировать будущую ошибку.
  `Core/Rendering/RenderGeometry.cs:129–141`.
- **Предупреждение сборки CS8604** — возможный null в `sourceId`.
  `Web/Controllers/MosaicController.cs:120`. Единственное предупреждение в сборке; закрыть,
  чтобы сборка была чистой.
- **Устаревшая подпись под результатом.** `Views/Mosaic/Result.cshtml`: «набранное панно
  сядет чуть глуше картона». Компенсацию шва выпилили, а шов рисуется прямо на картоне
  (L\* 40 на 22–25 % площади) — утверждение надо перепроверить и переписать или убрать.
- **Схема с номерами.** Коды в схеме — индексы легенды (1, 2, 3…), не артикулы. По решению
  пользователя схема нужна только для разбора алгоритма, так что это не дефект; зафиксировано,
  чтобы не всплывало снова.
