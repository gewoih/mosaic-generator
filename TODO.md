# Бэклог

Порядок — **по тому, что сильнее бьёт по глазу на картоне**, а не по величине числа.
Каждый пункт: в чём проблема, чем подтверждается, где в коде.

Цифры сняты 2026-09-04 стендом `tools/MosaicGenerator.Diag` по восьми фото из `samples/`
на размерах 15×15, A4 (21×29,7) и 30×30 см, откус 10 мм, 12 цветов, **после ограничения
пропорции куска** (`docs/proportsiya-kuska.md`) и **после привязки светлоты и штрафа за смену
тона** (`docs/tsvet-uezzhaet-plan.md`). Старые числа, снятые до этих двух правок, сравнивать
с нынешними нельзя. Разбор укладчика — `docs/kursy-obryvayutsya-plan.md` (стенд с `--why`).

**Судить по картону, а не по метрике** (правило в `CLAUDE.md`). Главный дефект зависит от
сюжета, и это определило порядок ниже:

| фото | что бросается в глаза первым |
|---|---|
| gull, gull-2, dolphin | крошево на текстурных деталях внутри силуэта (см. ниже) |
| human | фон лёг курсами, **лицо не читается** — деталь мельче куска (см. ниже) |
| landscape | берег/камни крошевом; небо при этом уложено курсами ровно |
| landscape-2 | цвет пришёл в норму; осталась самая шумная раскладка из восьми (пп. 2, 3) |
| gull-2, dolphin-2 | ровное небо одним артикулом на две трети панно (п. 5) |

Бывший пункт 1 («крошево там, где нет структуры») снят 2026-09-04: гипотеза проверена по всем
восьми фото (`docs/kroshevo-mekhanizm-a-plan.md`) и не подтвердилась. Дело не в отсутствии
структуры — плоский фон и ровная внутренность силуэта (небо, грудь чайки) укладываются курсами
нормально. Крошево концентрируется там, где у самой фотографии есть **своя мелкая текстура
тоньше масштаба куска** (оперение, камни, черты лица, брызги, рябь листвы) — противоположный
диагноз исходному. Лечение не начато: открытый вопрос — гасить ли эту текстуру на уровне поля
направлений/контуров, или это честная передача детали, которую стоит оставить. Разбор — там же.

**Закрыт 2026-09-05.** Текстуру гасим до сэмплирования: `Core/Imaging/ImageFlattener.cs`,
эллиптическое ядро вдоль формы, включено в вебе на 0,7 / ΔE 6 / ×2 / поперёк 0,25. Замеры —
`docs/ploskosti-spike.md` (изотропный спайк) и `docs/anizotropnoe-uploshchenie-plan.md`
(анизотропия). Крап 0,013 → 0,010, острова 0,037 → 0,028, излом 0,150 → 0,113, уход тона
0,197 → 0,084. Порождённый техдолг — пункт 1 ниже.

---

## Приёмка: как проверяется любой пункт этого файла

Правка алгоритма принимается **только** прогоном стенда по всем фото из `samples/`
на трёх размерах, кусок 10 мм, 12 цветов, и **обязательно двумя руками**, «до» и «после»,
снятыми одной сборкой подряд. На воротных кадрах (`gull`, `human`, `dolphin`) добавляется
60×60 — эталон, где видно, что даёт алгоритм при достатке места:

```bash
for f in samples/*.jpg; do n=$(basename "$f" .jpg)
  case "$n" in gull|human|dolphin) SZ=21x21,21x29.7,40x40,60x60 ;;
               *)                  SZ=21x21,21x29.7,40x40 ;; esac
  dotnet run -c Release --project tools/MosaicGenerator.Diag -- \
      --photo "$f" --out "diag-out/<задача>/<до|после>/$n" \
      --sizes "$SZ" --modules 10 --colors 12
done
```

Стенд с 2026-09-05 уплощает по умолчанию тем же, чем веб (`FlattenSettings.Production`);
`--flatten off` выключает. До этого флаг надо было передавать руками, и записанная здесь
команда его не несла — то есть мерила пайплайн без уплощения.

- **Числа из прошлых замеров не сравнивать.** База сбрасывалась дважды: оптика шва 2026-09-04,
  уплощение 2026-09-05. Обе руки снимаются заново.
- **Картоны смотреть глазами**, попарно до/после на одном кадре, а не только `metrics.csv`.
  Метрика лучше, а картон хуже — права картина, метрику пересмотреть.
- **Три кадра — ворота, общие для всех пунктов.** Сорваны — правка не принята,
  какими бы ни были числа:
  - `human` 40×40 — читаются бровь, глазница, нос, скула;
  - `gull` 40×40 — клюв остаётся клином, не пятном;
  - `dolphin` 40×40 — глаз пятном, спина серая, без бежевой полосы (уход тона ≤ 0,005).
- **Все кадры из `samples/`, не три.** Подгонка под один кадр — уже случившаяся ошибка.
  Набор пересобран 2026-09-05 по осям отказа, а не по сюжетам — десять кадров, разбор в
  `docs/chetyre-sglazhivatelya-plan.md`, раздел «Набор». Убраны `gull-3`, `landscape-2`,
  `dolphin-2`; добавлены `sunset` (единственный носитель `banding`), `poppy` (насыщенный цвет,
  резкая граница), `wave` (единственный носитель `kink`), `cat-fur` (мелкая текстура — шерсть),
  `cat-dusk` (тёмный низкий контраст). Числа в разборах ниже сняты по старому набору и старым
  размерам — сравнивать только рука с рукой.
- **Размеры пересмотрены 2026-09-05**: 21×21 вместо 15×15 и 40×40 вместо 30×30. На 15 см при
  куске 10 мм поперёк 13 тессер, и почти любой сюжет там нечитаем в принципе — это пункт 10,
  а не дефект укладки, и держать такой размер воротным значило судить алгоритм по кадру,
  который он не может вытянуть.
- Не должны сдвигаться в минус: доля доминирующего артикула (п. 5), доля четырёхугольников
  (п. 2), `uncuttable` (п. 8).

---

## 1. ~~Четыре сглаживателя воюют с одним врагом~~ — закрыт 2026-09-05

**Гипотеза не подтвердилась, оба этапа оставлены.** Разбор и цифры —
`docs/chetyre-sglazhivatelya-plan.md`, раздел «Результат».

Пункт считал, что `ImageFlattener` и `CellSmoother` бьют по одному врагу на разных масштабах.
Это неверно: уплощение сглаживает **фотографию**, а рассогласование, которое снимает смузер,
**рождается самим сэмплированием** — усреднения под соседними контурами расходятся на доли оттенка
даже с идеально гладкого фото, и выше тесселяции этого не видно, потому что там тессер ещё нет.
Снятие смузера стоит: одиночки 0,63 → 0,97 %, острова 1,60 → 2,42 %, и **срывает ворота на
дельфине** (уход тона 0,000 → 0,028, розовая полоса по спине на картоне).

Мёртвая зона `LocalContrast` пере-подобрана на чистом сигнале (0 / 1,0 / 1,5 / 2,5 / 3,5 / 5,0 ΔE,
33 прогона): вниз хуже, вверх формально лучше, но выигрыш куплен тем, что этап перестаёт работать —
слипание планов растёт монотонно. Оставлено 2,5. Комментарии обоих этапов переписаны под новые
замеры вместо ссылок на исчезнувшую посылку.

**Побочно починено:** стенд не уплощал по умолчанию, а команда приёмки флага не несла — то есть
любая рука, снятая по записанной процедуре, мерила пайплайн **без** уплощения. Теперь дефолт
стенда равен прод-настройкам (`FlattenSettings.Production`), `--flatten off` выключает.

**Четвёртый сглаживатель тоже проверен** отдельной рукой: `CoherentMap` в один проход даёт
крап +46 %, на дельфине белую плитку в тёмной полосе на спине. Остаётся; вывод для п. 11 — там же.

---

## 2. Форма куска: четырёхугольников половина, остальное — пяти- и шестиугольники

**Проблема.** Смальту берут кусачками или молотком: скол идёт сквозь пластину, кусок выходит
четырёхугольным. Пятиугольник со скруглённым углом руками не откусывается — новичок будет
либо подгонять полчаса на кусок, либо класть не то, что нарисовано, и раскладка поедет.

**Чем подтверждается** (после ограничения пропорции, `docs/proportsiya-kuska.md`): 4 угла —
**44–61 %** на семи кадрах и **33–37 % на landscape-2**, 5 углов — 31–43 %, 6 углов —
8–21 %, 7 и больше — 0–4 %. Вогнутых кусков 0 %, самопересекающихся 0 %, минимальный
внутренний угол p1 = 44–55° — куски выпуклые и не иглы, просто многоугольные. «Стрелки» и
«галочки» на картоне — это не куски, а дыры в шве (пункт 4).

**Половина уже взята пропорцией куска**: 35,5 → 47,0 % четырёхугольников в среднем по 24
прогонам, без единой правки в резке. Дальше этим ходом не добрать: потолок — около 51 %,
столько даёт даже прямой параллельный структурный курс. Вторую половину брать здесь, в
резке ячейки. Хуже всех по-прежнему landscape-2 (33 %) — самая шумная раскладка из восьми;
на нём и проверять.

**Где в коде.** `Core/Grid/Tessellation.cs`, `CutCells` — построение полигона тессеры между
соседними линиями тока. Ограничить до четырёхугольника (при необходимости — прямым
отрезанием лишних вершин), либо доказать, что 5–6 углов режется, и снять вопрос.

---

## 3. Крап: до 5,8 % одиночных кусков и до 11,6 % кусков в островах по 1–2 штуки

**Проблема.** На картоне landscape и human посреди ровного неба лежат одиночные белые и
чёрные куски. В стекле белый — самый громкий артикул, одна такая тессера в сером небе
читается как ошибка набора, а не как оттенок.

**Чем подтверждается.** Доля тессер, чей артикул не совпал ни с одним соседом (и из них —
«громкие», > 15 ΔE от среднего по соседям), плюс доля кусков в островах одного артикула
размером 1–2:

| фото / размер | одиночных | из них громких | в островах 1–2 |
|---|---|---|---|
| landscape-2 15×15 | **5,8 %** | **5,4 %** | **11,6 %** |
| gull 15×15 | 5,1 % | 4,3 % | 7,5 % |
| landscape-2 30×30 | 4,4 % | 3,3 % | 8,5 % |
| human 15×15 | 4,0 % | 2,5 % | 7,6 % |
| landscape 15×15 | 3,3 % | 2,9 % | 7,5 % |
| gull 30×30 | 1,1 % | 0,8 % | 2,4 % |

На панно 15×15 из ~250 кусков 5 % громких одиночек — это дюжина кусков, которые видно все.
Хуже всего на **15×15**, а это рабочий размер номер один. Привязка светлоты и штраф за тон
крап не тронули (2,4 → 2,0 % в среднем, худшее 5,4 → 5,8 %): дефект свой собственный.

**Где в коде.** `Core/Quantization/CoherentMap.cs` (`Settle`) вызывается дважды и должен это
гасить, но не гасит. Смотреть силу связи с соседями и то, что считает соседом
`CellNeighbourhood`: у затычки круг соседей другой, а затычек ещё 6–24 % панно
(`docs/kroshevo-mekhanizm-a-plan.md`).

---

## 4. Дыры в раскладке до 7,6 мм при номинальном шве 1 мм

**Проблема.** Именно эти дыры глаз читает как «стрелки» и «галочки» на картоне (куски тут ни
при чём, см. пункт 2). Набирая поверх картона, пользователь либо оставит дыру, либо начнёт
двигать соседей и уедет от раскладки.

**Чем подтверждается.** Ширина шва по площади шва (удвоенное расстояние до ближайшей
тессеры) при номинале 1,0 мм: p50 = **0,7–1,0 мм**, p90 = 1,5–2,0 мм, максимум
**4,7–7,6 мм**; площадь с зазором > 3 мм — 0,2–0,5 % панно; площадь шва целиком 22,5–25,8 %
при номинальных 20,5 % для 10×7 мм со швом 1 мм.

**С обрывом курса связано слабо — проверено.** Гипотеза «дыра там, где курс оборвался» не
подтвердилась: концентрация зазоров > 3 мм у торцов курсов ×1,3 (медиана), у затычек ×0,9 —
те их скорее закрывают, чем создают. Ограничение пропорции куска убрало пятую часть площади
широких зазоров, но максимальный зазор оставило прежним.

**Где в коде.** `Core/Grid/Tessellation.cs`, `CutCells` — дыра там, где соседние курсы
расходятся, а резка ячеек оставляет между ними клин.

---

## 5. Один артикул забирает до 64 % панно, нижняя половина артикулов — 8–14 % на всех

**Проблема.** Ровный фон, набранный одним артикулом на пол-панно, в стекле мёртв: смальту для
того и кладут курсами в 2–3 близких оттенках, чтобы поверхность играла. Сейчас бюджет в
12 цветов уходит на три артикула, а девять делят остаток.

**Чем подтверждается.**

| фото / размер | самый частый артикул | топ-3 | нижняя половина артикулов вместе |
|---|---|---|---|
| gull-2 15×15 | **66,5 %** | 80,9 % | 8,6 % |
| gull-2 30×30 | 66,2 % | 85,1 % | 7,2 % |
| gull-2 A4 | 57,8 % | 83,8 % | 6,5 % |
| dolphin-2 A4 | 52,4 % | 81,7 % | 8,4 % |
| gull 15×15 | 41,1 % | 77,9 % | 9,9 % |
| landscape-2 30×30 | 22,6 % | 43,1 % | 33,1 % |

Доминанта **росла дважды**. Ограничение пропорции: фон набирается более мелким куском, кусков
в ровном небе стало больше, и все они одного артикула. Привязка светлоты (`ToneMap`): у
gull-2 15×15 32 → 66 %. Прежнее разнообразие там было ложным — небо разваливалось на
несколько артикулов из-за неверной светлоты и выглядело грязным; теперь оно честно одного
тона. Дефект от этого не появился, а обнажился: делить ровное поле надо намеренно.

**Где в коде.** Свёртка (`Core/Quantization/PaletteReducer.cs`) скорее не виновата: она роняет
оттенок по стоимости ошибки, а ровное небо ронять некуда — там уже один оттенок. Смотреть
выше — `ToneMap` растягивает глобально и не может разделить то, чего не разделила фотография
(`docs/tsvet-uezzhaet-plan.md`). Нужен явный ход мозаичиста: делить крупное ровное поле на
2–3 соседних артикула по курсам, а не по шуму.

**Прежде чем ставить цель** — померить дно: на 12 оттенках какая-то доминанта неизбежна.

---

## 6. Бордюр съедает 18–20 % кусков на 15×15 и рвётся по краям

**Проблема.** На самом ходовом размере пятая часть всей работы уходит в декоративную рамку.
Плюс на dolphin и dolphin-2 бордюр местами отсутствует: куски выходят к самому краю поля и
упираются в широкий зазор.

**Чем подтверждается.** «Кусков в бордюре»: 15×15 — **17,8–20,4 %**; A4 — 11,9–12,6 %;
30×30 — 9,8–10,4 %. Разрывы по краю видны глазом на `dolphin/15x15`, `dolphin-2/15x15`.

**Где в коде.** `Core/Grid/Tessellation.cs`, `BorderCourseCount` и укладка колец (`RectRing`).
Проверить, от чего считается доля поля и почему на части фото кольцо не замыкается.

---

## 7. Картон печатается без легенды, кодов, контура панно и масштабной линейки

**Проблема.** Пользователь определяет артикул на глаз по цвету картона и сверяется с экраном.
Это работает, пока 10–15 артикулов различимы **на печати**, а сейчас ничто этого не
гарантирует: свёртка выбирает оттенки по стоимости ошибки, и два близких серых легко проходят
вместе. Плюс на листе нет ни контура панно, ни линейки, чтобы проверить масштаб печати.

**Что делать.**
1. **Полоса легенды на самом картоне**, за пределами поля мозаики: плашка артикула, его номер
   и число кусков. Тогда картон самодостаточен.
2. **Контрольная линейка 100 мм и контур панно** — сразу видно, если принтер отмасштабировал.
3. **Гарантия различимости**: не выпускать в набор две смальты ближе некоторого ΔE; если
   свёртка их оставила — слить либо предупредить в интерфейсе.
4. Коды в каждой тессере не нужны: при 10–15 цветах они забьют лист.

**Где в коде.** `Core/Skia/SkiaMosaicRenderer.RenderCartoon`, `Core/Rendering/RenderOptions.Cartoon`,
`Views/Mosaic/Result.cshtml`.

---

## 8. Куски уже 5 мм: p1 узкой стороны 4,0–4,5 мм

**Проблема.** Замерами с натуры порог узкой стороны — 5 мм: уже этого кусок не поставить.
Сейчас 1 % кусков за порогом, 5 % — на самой границе.

**Чем подтверждается.** Ширина куска (минимум по направлениям, а не по осям курса):
p1 = **4,03–4,48 мм**, p5 = 4,43–4,87 мм. Стенд по своей мерке (`minSideP5` вдоль оси курса)
даёт 4,7–5,2 мм. Расхождение между двумя мерками само по себе стоит свести.

**Где в коде.** `Core/Grid/Tessellation.cs` — отсечение слишком узких кусков;
`tools/MosaicGenerator.Diag/Metrics.cs` — `Extent` меряет вдоль оси курса, а не по кратчайшей
ширине полигона.

---

## 9. Зон и границ нет: цветовая половина работает с супом тессер

**Проблема.** Между «куски нарезаны» и «красим» нет ни одного этапа, знающего о зонах
изображения. Вся цветовая половина видит неупорядоченный набор тессер плюс граф соседства.
Поэтому opus vermiculatum существует только вокруг силуэта — там, где `ContourSet` нашёл
контур, — и больше нигде: граница щеки и тени, кромка волны, край камня алгоритму как объекты
неизвестны.

**Чем подтверждается.** Мозаика читается парой «плоскость + контур», и обе половины у нас
неполны. Плоскости даёт уплощение, но мягкие; контуров нет вовсе. Прямое следствие замерено:
на анизотропном радиусе 1,0 укладка лучше (излом 0,108, острова −34 %), но фильтр мажет через
границу серого и бежевого — уход тона 0,084 → 0,140, у дельфина по спине бежевая полоса. Он
не знает, что там граница. Поэтому радиус и пришлось оставить на 0,7
(`docs/anizotropnoe-uploshchenie-plan.md`).

**Что сделать.** Сегментация с явными границами — L0 gradient minimization либо
mean-shift / SLIC. Даёт два выхода: настоящие плоские области (сильнее мягких плато) и
**список границ**. Границы затем (а) запрещают фильтру размывать через них, что снимает
потолок радиуса, и (б) ведут курс вдоль себя, а куску запрещают себя пересекать.

**Приёмка — по правилам выше**, плюс: радиус уплощения должен подняться до 1,0–1,5 при уходе
тона не хуже 0,084, иначе ход не окупился.

**Где в коде.** Новый этап между `ImageFlattener` и `DirectionField`;
`Core/Grid/ContourSet.cs` и `CourseGuidance` получают границы вместо одного силуэта.
План разговора — `docs/ploskosti-spike.md`, раздел «Вывод».

---

## 10. Пайплайн молча выдаёт кашу, когда сюжет не влезает в размер

**Проблема.** Рабочий процесс — «нашёл картинку в интернете, сделал картон». `landscape-2` на
15×15 отрабатывает честно и выдаёт нечитаемую раскладку, ни словом не предупредив, что
15 тессер поперёк для этого сюжета мало в принципе.

**Чем подтверждается.** Проверено 2026-09-05 на всех восьми фото: `landscape-2` 15×15 не
читается ни до уплощения, ни после — регресса нет, но и читаемости нет. Никакой фильтр этого
не чинит: деталей нужного масштаба в панно физически не существует.

**Что сделать.** Проверка на входе: сравнить масштаб значимой детали фотографии с размером
куска и сказать словами — «этому сюжету нужно от 30 см либо более тесный кроп». Это,
вероятно, полезнее для дела, чем следующий этап обработки: чинит выбор кадра, а не картон.

**Приёмка.** Проверка обязана пропустить `gull`/`dolphin` на 30×30 и остановить `landscape-2`
на 15×15 — иначе она либо бесполезна, либо мешает.

**Где в коде.** Новая проверка рядом с `Core/Validation/MosaicRequestValidator.cs`, но с
доступом к фотографии; предупреждение показывать в `Web/Views/Mosaic/`.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 30×30 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Печать больше A4 пока не планируется, но склейка понадобится: разрезка на
листы с перекрытием, метками совмещения и номером листа; лучше PDF, чтобы масштаб не зависел
от просмотрщика.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 30×30 см →
3600 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.

---

## 11. Мелочи и техдолг

- **`RenderGeometry.Clamp` — мёртвый код.** Прижимает вершины тессеры к границе поля, но
  замерено: 0 % кусков выходит за поле на всех восьми фото. Либо убрать, либо превратить в
  проверку с падением. `Core/Rendering/RenderGeometry.cs:129–141`.
- **Предупреждение сборки CS8604** — возможный null в `sourceId`,
  `Web/Controllers/MosaicController.cs:120`. Единственное предупреждение в сборке.
- **Устаревшая подпись под результатом.** `Views/Mosaic/Result.cshtml`: «набранное панно сядет
  чуть глуше картона». Компенсацию шва выпилили, а шов рисуется прямо на картоне — утверждение
  перепроверить и переписать или убрать.
- **`CoherentMap` гоняется дважды — диагноз подтверждён замером 2026-09-05.** `PaletteReducer`
  переквантовывает осиротевшие ячейки поодиночке и заново вносит рассогласование, которое первый
  проход только что убрал. Рука с одним проходом (33 прогона): одиночки 0,63 → 0,92 %, громкие
  0,41 → 0,61 %, острова 1,60 → 2,05 %; на картоне `dolphin` 40×40 белая тессера садится в тёмную
  полосу на спине. То есть **второй проход снимать нельзя** — он делает настоящую работу, и это
  доказывает, что редуктор рассогласование действительно создаёт. Чинить внутри редуктора, проход
  снять после. Разбор — `docs/chetyre-sglazhivatelya-plan.md`, раздел про руку E.
  `Core/Pipeline/MosaicGenerationService.cs`, `Core/Quantization/PaletteReducer.cs`.
- **`MaxColors` значит две разные вещи.** В `ToneMap` это тональный шаг (насколько растягивать),
  в `PaletteReducer` — счёт артикулов. Следствие неочевидное: меняя 12 цветов на 15, меняешь и
  силу растяжки, а не только палитру. Либо развести на два понятия, либо написать это в
  подсказке к полю в вебе.
- **Тензор считается дважды за прогон** — флаттенером по сырому фото, `DirectionField` по
  уплощённому. Это намеренно (разные картинки, разные вопросы), не дубль; записано, чтобы
  никто не «оптимизировал».
