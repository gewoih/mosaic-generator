## Приёмка

Процедура, на которую ссылается `CLAUDE.md`. Держится только здесь.

**Прогон.** Все фото из `samples/` (10 штук), три размера, откус 5 мм, 10 цветов —
30 прогонов, около трёх секунд каждый, весь набор — две минуты:

```bash
for f in samples/*.jpg; do n=$(basename $f .jpg); \
  dotnet run -c Release --project tools/MosaicGenerator.Diag -- \
    --photo $f --out diag-out/priyomka/$n --sizes 20x20,30x30,40x40 \
    --modules 5 --colors 10; done
```

Мерить по всем десяти, а не по трём: настройки однажды оказались подогнаны под `gull.jpg`
(`docs/diagnostika-chayka.md`). Сравнение — только «до/после на одном кадре»: снять базу
тем же прогоном **до** правки, дальше сравнивать построчно по паре (фото, размер).

**База 2026-09-07**, снята после правки п. 4б и сверки палитры по вееру (`b4dc27b`),
пересняты `dE_mean`, `smallIsland`/`midIsland` и `merged` после сверки по витрине
(`docs/palitra-sverka-po-sajtu.md`: `SX42` и `FN10`). Прежние цифры ΔE несравнимы —
база сброшена вместе с HEX. Строки геометрии не пересчитывались: палитра на раскладку
не влияет, все восемь колонок побитово те же в 30 прогонах до и после.

Пункт 18 (2026-09-07) добавил колонки `autoColors` / `kneeRatio` / `ceilingColors`, но
геометрию и цвет приёмки не сдвинул: при `KneeFactor = 3.5` колено на всех фото `samples/`
отдаёт потолок (12), то есть рендер идёт при том же числе цветов, что и раньше. Пересматривать
базу нужно будет вместе с калибровкой `KneeFactor`.

| колонка | медиана | p90 | max | что ловит |
|---|---|---|---|---|
| `wedgeFill` | **0,0000** | 0,0000 | 0,0008 | клинья, куда кусок влез бы, но не поставлен |
| `wedgeLegal` | 0,0178 | 0,0233 | 0,0243 | клинья, куда кусок не поставить: дно, заданное материалом |
| `jointArea` | 0,2354 | 0,2440 | 0,2474 | весь шов; **не судья** — 99 % лишка над номиналом 0,205 диктует материал |
| `narrowWidth` | 0,0624 | 0,1211 | 0,1519 | доля кусков уже 5 мм — руками не отколоть |
| `minWidthP5` | 4,90 мм | 5,47 | 5,60 | узкая сторона по нижнему хвосту |
| `filler` | 0,0572 | 0,0841 | 0,0926 | рябь из затычек в фоне |
| `awkward` | 0,0338 | 0,0721 | 0,0778 | неудобные для откуса куски |
| `kink` | 0,0978 | 0,1506 | 0,1848 | излом курса |
| `overlap` | 0,0009 | 0,0017 | 0,0022 | куски заезжают друг на друга |
| `bare03` | 0,0025 | 0,0074 | 0,0086 | голое поле больше трети модуля |
| `dE_mean` | 10,47 | 15,18 | 22,82 | уход цвета; дно задаёт палитра, а не алгоритм |
| `smallIsland` / `midIsland` | 0,0126 / 0,0658 | 0,0322 / 0,1171 | 0,0512 / 0,1813 | крап: острова 1–2 и 3–10 кусков |
| `merged` | 0,4408 | 0,5118 | 0,6669 | доля переназначенного при редукции |

**Сорванные ворота — по числам.** Любое из этого останавливает правку:

- **Правка цвета сдвинула хоть одну колонку геометрии.** Раскладка считается до подбора цвета
  (`Tessellation.Advected` идёт раньше `Quantizer.Map`), поэтому правка в `ColorDistance`,
  `Quantizer`, `CoherentMap` или `PaletteReducer` обязана оставить всю геометрию **побитово**
  неподвижной: max |Δ| = 0 по всем 24 колонкам. Проверено на 30 прогонах × шесть вариантов
  подбора (`docs/neytralnaya-tsel-plan.md` §4). Самый дешёвый ранний гейт: сорвался — правка
  залезла не туда, дальше мерить нечего.
- `uncuttable`, `manySided`, `concave` перестали быть нулями. Сейчас все три — ровно 0
  во всех 30 прогонах; кусок, который не выкусывается, семиугольник и вогнутый кусок
  запрещены физикой материала, а не вкусом.
- `narrowWidth` вырос по медиане, или `minWidthP5` упал ниже 4,5 мм — панно набирается
  крошевом, которого руками не наколоть.
- `bare03` или `overlap` выросли вдвое — куски не сходятся или лезут друг на друга.
- `wedgeFill` перестал быть нулём по медиане — на панно появились клинья, куда кусок влезал бы,
  но не поставлен. Смотреть их картой `--holes`: они обведены на ней кружком.
- Контроль `sunset` (плоское поле, курсы прямые и параллельные) уехал больше чем на 0,001
  по `jointArea`: сейчас 0,2017 / 0,2053 / 0,2030 при номинале 0,205. Если сдвинулся он —
  правка задела не кривизну, а саму резку.

**Ворота глазами — картон, а не число** (`CLAUDE.md`, «Судья — картон, а не метрика»).
`Read` по PNG из прогона, до и после, на одном кадре:

- `gull` 21×21 — клюв должен читаться пятном, фон вокруг силуэта без треугольных клиньев;
- `dolphin` A4 — спина ровная, без крапа: **сиреневая полоса по верху тела дефектом не
  является** и воротами не служит — фотография там сиреневая, артикул точен в 3,4 ΔE
  (замер 2026-09-07, `docs/neytralnaya-tsel-plan.md`). Воротный порог «уход тона ≤ 0,005»
  судьёй быть не может;
- `human` 40×40 — лицо читается: очки тёмной полосой, борода не рассыпана в крошево.
  Белая цепочка слева и белое пятно внизу — **не дефект**: это узор на колпаке собаки
  и край стола из фотографии (проверено по оригиналу 2026-09-07);
- `poppy` 21×21 — лепестки красные, не лососевые (сюда упирается `dE_mean` 22,8: это
  потолок веера, а не дефект раскладки);
- `sunset` 40×40 — контроль: курсы прямые, ни одного клина.

**Что не является воротами.** `v4` (доля четырёхугольников) — падает всякий раз, когда
клин заменяется выпуклым пятиугольником, то есть при улучшении; смотреть его только вместе
с `awkward` и `minWidthP5`. **`jointArea`** — 58 % её лишка над номиналом это клинья, куда
кусок физически не поставить, ещё 41 % — шов чуть шире номинала; мерить надо `wedgeFill`
(замер 2026-09-07, разбор — `docs/klin-na-tortse-kursa-plan.md`, там же список того, что
по клиньям уже опровергнуто и повторять не нужно). **`midIsland`** (острова 3–10 кусков) —
ловит законные пятна картинки, а не крап: на худшем кадре набора (`human` 21×21, 0,1645)
глазами это узор на колпаке собаки, стол и текстура кирпичной стены из фотографии.
Проверено 2026-09-07 и раньше 2026-09-06 связкой редукции, которая метрику не двинула.
**Жёсткий median-проход по островам 3–10 отвергнут:** при медиане 0,070 он съест реальные
детали. Одиночки и пары — другое дело, они закрыты правкой `CoherentMap`
(`docs/krap-odinochki-plan.md`), и `singleton` расти не должен.
«Курсов < 3 кусков» — не дефект (проверено 2026-09-04).
**Сиреневая полоса по спине `dolphin`** — не дефект подбора и не ворота: фотография там
сиреневая при C\* 11, артикул точен в 3,4 ΔE, до ближайшего серого 20 ΔE. Разобрано и закрыто
2026-09-07, `docs/neytralnaya-tsel-plan.md` — там же список того, что по ней уже опровергнуто
(правка `ColorDistance`, `HueWeight`, отбор дюжины, стягивание к нейтральной оси) и чего
повторять не нужно. «Уход тона на дельфине» — оттуда же.
`merged` 0,44 — дно от 12 оттенков.

---

## 18. Калибровка `KneeFactor` авто-подбора цветов

Механика авто-подбора сделана 2026-09-07 (`docs/avto-podbor-cvetov-plan.md`): `ReductionLadder`,
`Knee`, стрелки над картоном, колонки `autoColors` / `kneeRatio` / `ceilingColors`. Осталась
настройка порога.

**Проблема.** При `KneeFactor = 3.5` с накопленной медианой колено на реальных фото почти
всегда отдаёт потолок — редукция со 152-цветной палитры стартует с почти нулевых сбросов
дублей, дальше любой сброс «дорог» относительно медианы, колено срабатывает сразу и
зажимается потолком. Прогон 21×21 и 40×40 по gull/dolphin/human/landscape/sunset — все
`авто 12/12`, только `poppy` 21×21 дал 11/12. Механизм безопасен («лишний цвет безопаснее
сюжета»), но пока инертен.

**Что сделать.** Подобрать `KneeFactor` — и, возможно, заменить накопленную медиану
скользящим окном последних N ступеней, чтобы плавный подъём предельной стоимости не
считался изломом, — прогоном по всем `samples/` с воротами глазами (`human` 40×40
бровь/глазница, `gull` 40×40 клюв пятном, `dolphin` 40×40 серая спина). Сигнал — колонка
`kneeRatio` (сейчас 3,5–8,0). Базу приёмки пересматривать вместе с этой правкой.

**Где в коде.** `Core/Quantization/ReductionLadder.cs` (`Knee`, `KneeFactor`).

---

## 21. Обратный набор: картон и схему некому зеркалить

**Проблема.** При обратном наборе тессеры кладутся лицом вниз на временную основу поверх
картона, и готовая работа выходит **зеркальной** тому, что напечатано. Зеркального вывода нет:
`grep` по `src`, `tools`, `docs` на mirror / зеркал / flip не находит ничего. Весь рендер
спроектирован под прямой набор, это записано в `RenderOptions.Cartoon`: «printed 1:1 and set
on directly».

**Чем подтверждается.** 2026-09-07: работа 20×20 см по `gull-2` набирается обратным способом,
печать 1:1 на A4. На симметричном сюжете расхождение не видно, на чайке клюв развернётся
в другую сторону — и поправить после переноса нечем.

**Что сделать.** Флаг в `RenderOptions` — не настройка на каждый запуск, а способ набора, он
у мастера один на работу. Зеркалится **геометрия**; номера на схеме и линейка 100 мм рисуются
поверх уже отражённого поля и не зеркалятся, иначе схема нечитаема. Легенда не зеркалится
вовсе — отдельный лист. Проверка: наложить зеркальный картон на прямой — совпадение по
вертикальной оси, и все цифры на схеме читаются слева направо.

**Где в коде.** `Core/Rendering/RenderOptions.cs`, `Core/Skia/SkiaMosaicRenderer.cs`
(`RenderCartoon`, `RenderScheme`), `Core/Rendering/CartoonSheet.cs` (линейка),
`Web/Controllers/MosaicController.cs` + форма.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 40×40 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Понадобится: разрезка на листы с перекрытием, метками совмещения и номером
листа; лучше PDF, чтобы масштаб не зависел от просмотрщика. 20×20 см влезает на A4 целиком,
так что до панно крупнее A4 пункт не горит.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 40×40 см →
4800 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.
