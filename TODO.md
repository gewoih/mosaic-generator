# Бэклог

Порядок — **по тому, что сильнее бьёт по глазу на картоне**, а не по величине числа.
Каждый пункт: в чём проблема, чем подтверждается, где в коде.

Цифры сняты 2026-09-05 стендом `tools/MosaicGenerator.Diag` по десяти фото из `samples/`
на размерах 21×21, A4 (21×29,7) и 40×40 см (на воротных кадрах ещё 60×60), откус 10 мм,
12 цветов, на прод-настройках уплощения. Что с чем можно сравнивать — в «Приёмке» ниже.
Разбор укладчика — `docs/kursy-obryvayutsya-plan.md` (стенд с `--why`).

**Номера пунктов не переиспользуются.** На них ссылаются комментарии в коде и разборы в
`docs/`, поэтому закрытый пункт просто исчезает, а его номер остаётся пустым; оставшиеся
не перенумеровываются. История закрытых — в `docs/*.md` и в git.

**Судить по картону, а не по метрике** (правило в `CLAUDE.md`). Главный дефект зависит от
сюжета, и это определило порядок ниже:

| фото | что бросается в глаза первым |
|---|---|
| gull, gull-2 | ровное небо одним артикулом на пол-панно (п. 5) |
| dolphin | тело почти нейтральное и перекидывается в бежевый (п. 9б) |
| human | фон лёг курсами, лицо читается; крап в тенях (п. 3) |
| landscape | берег и камни — самая шумная раскладка набора (п. 3) |
| wave | крошево на гребне и брызгах; половина ухода тона по набору |
| human на 21×21 | лицо нечитаемо в принципе — это п. 10, а не дефект укладки |

---

## Приёмка: как проверяется любой пункт этого файла

Правка алгоритма принимается **только** прогоном стенда по всем фото из `samples/`
на трёх размерах, кусок 10 мм, 12 цветов, и **обязательно двумя руками**, «до» и «после»,
снятыми одной сборкой подряд. На воротных кадрах (`gull`, `human`, `dolphin`) добавляется
60×60 — эталон, где видно, что даёт алгоритм при достатке места:

```bash
for f in samples/*.jpg; do n=$(basename "$f" .jpg)
  case "$n" in gull|human|dolphin) SZ=21x21,21x29.7,40x40,60x60 ;;
               *)                  SZ=21x21,21x29.7,40x40 ;; esac
  dotnet run -c Release --project tools/MosaicGenerator.Diag -- \
      --photo "$f" --out "diag-out/<задача>/<до|после>/$n" \
      --sizes "$SZ" --modules 10 --colors 12
done
```

Стенд с 2026-09-05 уплощает по умолчанию тем же, чем веб (`FlattenSettings.Production`);
`--flatten off` выключает. До этого флаг надо было передавать руками, и записанная здесь
команда его не несла — то есть мерила пайплайн без уплощения.

- **Числа из прошлых замеров не сравнивать.** База сбрасывалась трижды: оптика шва 2026-09-04,
  уплощение 2026-09-05, радиус уплощения 0,7 → 1,0 2026-09-05. Обе руки снимаются заново.
- **Картоны смотреть глазами**, попарно до/после на одном кадре, а не только `metrics.csv`.
  Метрика лучше, а картон хуже — права картина, метрику пересмотреть.
- **Три кадра — ворота, общие для всех пунктов.** Сорваны — правка не принята,
  какими бы ни были числа:
  - `human` 40×40 — читаются бровь, глазница, нос, скула;
  - `gull` 40×40 — клюв остаётся клином, не пятном;
  - `dolphin` 40×40 — глаз пятном, спина серая, без бежевой полосы. **Глазами, не по числу:**
    уход тона на этом кадре немонотонен и переворачивается от любой правки (пункт 9б).
- **Все кадры из `samples/`, не три.** Подгонка под один кадр — уже случившаяся ошибка.
  Набор пересобран 2026-09-05 по осям отказа, а не по сюжетам — десять кадров, разбор в
  `docs/chetyre-sglazhivatelya-plan.md`, раздел «Набор». Убраны `gull-3`, `landscape-2`,
  `dolphin-2`; добавлены `sunset` (единственный носитель `banding`), `poppy` (насыщенный цвет,
  резкая граница), `wave` (единственный носитель `kink`), `cat-fur` (мелкая текстура — шерсть),
  `cat-dusk` (тёмный низкий контраст). Числа в пунктах ниже пересняты 2026-09-05 по нынешнему
  набору и нынешним размерам; числа внутри разборов в `docs/` — нет, там сравнивать только
  рука с рукой.
- **Размеры пересмотрены 2026-09-05**: 21×21 вместо 15×15 и 40×40 вместо 30×30. На 15 см при
  куске 10 мм поперёк 13 тессер, и почти любой сюжет там нечитаем в принципе — это пункт 10,
  а не дефект укладки, и держать такой размер воротным значило судить алгоритм по кадру,
  который он не может вытянуть.
- Не должны сдвигаться в минус: доля доминирующего артикула (п. 5), доля четырёхугольников
  (`docs/forma-kuska-plan.md`), `uncuttable` (п. 8).
- **База на нынешнем наборе, размерах и радиусе 1,0**, снята 2026-09-05, 33 прогона, медианы
  (уход тона — сумма): излом 0,102, филлеры 0,072, одиночки 0,0052, громкие 0,0019, острова
  0,0133, четырёхугольников 0,522, dE_mean 9,51, доминанта 0,224, `uncuttable` 0,000, banding
  0,040, merged 0,436, уход тона 0,1230. На радиусе 0,7 (до 2026-09-05) было соответственно
  0,105 / 0,074 / 0,0056 / 0,0034 / 0,0138 / 0,513 / 9,77 / 0,233 / 0,000 / 0,048 / 0,445 /
  0,1131 — **с этими числами не сравнивать**, база сброшена третий раз. Уход тона на две трети
  приходится на `dolphin` и `wave`, и на обоих он немонотонен (пункт 9б).

---

## 3. Крап в островах 3–10 кусков: синий/белый блоб на лице (одиночки закрыты 2026-09-06)

**Одиночки и пары закрыты 2026-09-06** (`docs/krap-odinochki-plan.md`, вариант A′).
`CoherentMap` получил ветку для громкой истинной одиночки: кусок без единого одноартикульного
соседа, чей артикул дальше 10 ΔE от артикула каждого соседа, снимается на моду соседей
(`IsolationNeighbourWeight`, `IsolationMinRing`, `IsolationLoudGap`). Замер по всем `samples/`:
`human` 40×40 одиночки 0,8 → 0,4 %, громкие 0,4 → 0,2 %, острова 1–2 — 2,8 → 1,8 %; `dolphin`
A4 одиночки 0,7 → 0,1 %; сиреневые одиночки в воде и синий крап на макушке `human` ушли,
ворота (клюв чайки пятном, спина дельфина серая) целы. Цена: `merged` +0,01–0,02 в трети
строк, `gull` A4/40×40 +2–5 кусков в одиночках (на картоне не читается).

**Что осталось.** На картоне `human` 40×40 в тени щеки/на плече — **синий и почти белый
островки по 6–12 кусков**. Это не одиночка (внутри острова у куска есть одноартикульные
соседи), механизм п. 3 их не трогает намеренно. Дефект ближе к:

- **п. 5** — прохладный теневой артикул, который следовало слить с соседним, но `PaletteReducer`
  оставил как отдельный, потому что кусков набралось на «блоб»;
- **п. 13** — редуктор раздаёт осиротевшие ячейки поштучно и может собрать из них связное
  пятно не на своём месте.

**Чем подтверждается** (33 прогона, `posle/` 2026-09-06). Доля кусков в островах одного
артикула размером 1–2 (одиночки почти обнулены):

| фото / размер | в островах 1–2 |
|---|---|
| human 21×21 | **3,5 %** |
| human A4 | 3,5 % |
| wave A4 | 3,1 % |
| landscape A4 | 1,8 % |
| human 40×40 | 1,8 % |

**Что сделать.** Мерка островов 3–10 в стенде (сейчас метрика ловит только ≤ 2). Затем
решить, чинится ли это в `PaletteReducer` (не дробить прохладную тень на отдельный артикул
ниже порога площади) или это тот же техдолг, что п. 13. Отдельный план.

**Где в коде.** `Core/Quantization/PaletteReducer.cs`, `Core/Quantization/CoherentMap.cs`
(ветка одиночки уже есть — расширять на связные пятна рискованно: съест мелкие детали),
`tools/MosaicGenerator.Diag/Metrics.cs` (`NeighbourArticles` — поднять потолок острова).

---

## 4. Дыры в раскладке до 7,2 мм при номинальном шве 1 мм

**Проблема.** Именно эти дыры глаз читает как «стрелки» и «галочки» на картоне (куски тут ни
при чём — форма куска разобрана в `docs/forma-kuska-plan.md`). Набирая поверх картона,
пользователь либо оставит дыру, либо начнёт двигать соседей и уедет от раскладки.

**Чем подтверждается** (33 прогона, 2026-09-05). Ширина шва по площади шва (удвоенное
расстояние до ближайшей тессеры) при номинале 1,0 мм: p50 = **0,71–1,00 мм**,
p90 = 1,0–2,0 мм, максимум **3,8–7,2 мм**; площадь с зазором > 3 мм — 0,0–0,4 % панно;
площадь шва целиком 19,9–24,7 % при номинальных 20,5 % для 10×7 мм со швом 1 мм.

**С обрывом курса связано слабо — проверено.** Гипотеза «дыра там, где курс оборвался» не
подтвердилась: концентрация зазоров > 3 мм у торцов курсов ×1,3 (медиана), у затычек ×0,9 —
те их скорее закрывают, чем создают. Ограничение пропорции куска убрало пятую часть площади
широких зазоров, подъём радиуса уплощения снял ещё: максимум 7,6 → 7,2 мм, нижняя граница
площади широких зазоров дошла до нуля. Максимальный зазор всё ещё вдвое шире куска.

**Где в коде.** `Core/Grid/Tessellation.cs`, `CutCells` — дыра там, где соседние курсы
расходятся, а резка ячеек оставляет между ними клин.

---

## 5. Один артикул забирает до 66 % панно, нижняя половина артикулов — 6–12 % на всех

**Проблема.** Ровный фон, набранный одним артикулом на две трети панно, в стекле мёртв:
смальту для того и кладут курсами в 2–3 близких оттенках, чтобы поверхность играла. Сейчас
бюджет в 12 цветов уходит на три артикула, а девять делят остаток.

**Чем подтверждается** (33 прогона, 2026-09-05; все прогоны используют все 12 артикулов):

| фото / размер | самый частый артикул | топ-3 | нижняя половина артикулов вместе |
|---|---|---|---|
| gull-2 40×40 | **66,1 %** | 87,2 % | 6,2 % |
| gull-2 21×21 | 63,2 % | 85,3 % | 7,4 % |
| gull-2 A4 | 59,6 % | 87,0 % | 5,9 % |
| cat-fur 21×21 | 42,8 % | 74,8 % | 12,5 % |
| gull 21×21 | 42,3 % | 78,4 % | 8,3 % |
| landscape 40×40 | 22,4 % | 44,7 % | 31,0 % |

Медиана доминанты по набору — 22 %; проблема не всеобщая, а у сюжетов с большим ровным полем.
`landscape` показывает, каким распределение бывает, когда сюжету есть что делить.

**Доминанта росла дважды и один раз упала.** Ограничение пропорции: фон набирается более мелким
куском, кусков в ровном небе больше, и все одного артикула. Привязка светлоты (`ToneMap`):
у gull-2 32 → 66 %. Прежнее разнообразие было ложным — небо разваливалось на несколько
артикулов из-за неверной светлоты и выглядело грязным. Подъём радиуса уплощения 0,7 → 1,0 дал
обратный ход: медиана 0,233 → 0,224. Дефект от этого не исчез, а обнажился: делить ровное поле
надо намеренно.

**Где в коде.** Свёртка (`Core/Quantization/PaletteReducer.cs`) скорее не виновата: она роняет
оттенок по стоимости ошибки, а ровное небо ронять некуда — там уже один оттенок. Смотреть
выше — `ToneMap` растягивает глобально и не может разделить то, чего не разделила фотография
(`docs/tsvet-uezzhaet-plan.md`). Нужен явный ход мозаичиста: делить крупное ровное поле на
2–3 соседних артикула по курсам, а не по шуму.

**Прежде чем ставить цель** — померить дно: на 12 оттенках какая-то доминанта неизбежна.

---

## 6. Бордюр съедает 14 % кусков на 21×21, и его доля немонотонна по размеру

**Проблема.** На самом ходовом размере седьмая часть всей работы уходит в декоративную рамку.
Плюс доля не убывает с ростом панно, как должна бы: на 40×40 бордюр берёт 7 %, а на 60×60
снова 10 %. Значит, число колец считается не от того, от чего кажется.

**Чем подтверждается** (2026-09-05, `gull`, `dolphin`, `human`, `landscape` — доля почти не
зависит от сюжета, она геометрическая):

| размер | кусков в бордюре |
|---|---|
| 21×21 | **12,9–13,7 %** |
| A4 | 11,1–11,9 % |
| 40×40 | **7,0–7,6 %** |
| 60×60 | 9,4–9,9 % |

Прежние 17,8–20,4 % относились к 15×15 — размеру, убранному из приёмки.

**Разрывы кольца — перепроверить.** Прежнее наблюдение «на dolphin и dolphin-2 бордюр местами
отсутствует» снималось на 15×15 и на кадре, которого больше нет в наборе. На нынешних кадрах
глазами не встречено; либо подтвердить на 21×21, либо снять пункт.

**Где в коде.** `Core/Grid/Tessellation.cs`, `BorderCourseCount` (число колец берётся из
бюджета площади `BorderAreaBudget` — вероятная причина немонотонности) и укладка колец
(`RectRing`).

---

## 8. Куски на самой границе 5 мм: p5 узкой стороны 4,7–6,0 мм

**Проблема.** Замерами с натуры порог узкой стороны — 5 мм: уже этого кусок не поставить.
Сейчас за порогом почти никого, но пятая процентиль сидит прямо на нём.

**Чем подтверждается** (33 прогона, 2026-09-05, мерка стенда `minSideP5` вдоль оси курса):
p5 = **4,73–5,97 мм**; `uncuttable` 0,0–0,1 %; «крошек» (`tiny`) 0–3,7 %, «щепок» (`sliver`)
0–0,6 %. Прежние числа по кратчайшей ширине полигона (p1 = 4,0–4,5 мм) снимались отдельной
меркой и по старому набору — **расхождение между двумя мерками само по себе стоит свести**,
иначе непонятно, какая из них говорит про руку мастера.

**Где в коде.** `Core/Grid/Tessellation.cs` — отсечение слишком узких кусков;
`tools/MosaicGenerator.Diag/Metrics.cs` — `Extent` меряет вдоль оси курса, а не по кратчайшей
ширине полигона.

---

## 9б. Почти нейтральный цвет перекидывается между серым и бежевым артикулом

**Проблема.** Серо-мау́вовое тело дельфина стоит ровно на границе между серым артикулом и
бежевым. Любое возмущение где угодно выше по пайплайну перекидывает через эту границу целую
полосу тессер, и на картоне по спине идёт бежевая или фиолетовая полоса. Пользователь видит
не «оттенок чуть мимо», а другой цвет животного.

**Чем подтверждается.** Уход тона на `dolphin` по радиусу уплощения, тонкая сетка:

| размер | 0,7 | 0,85 | 1,0 | 1,2 | 1,35 | 1,5 |
|---|---|---|---|---|---|---|
| 21×21 | 0,000 | 0,000 | **0,030** | 0,000 | **0,022** | **0,026** |
| A4 | 0,000 | 0,000 | 0,004 | **0,024** | **0,027** | 0,000 |

Скачки между нулём и тремя процентами без тренда, на двух размерах в противофазе. Проверено
глазами: `dolphin` A4 на 1,2 — бежевая полоса по спине и фиолетовое тело; он же на 1,5 —
чистое серое. Родственный разбор — `docs/tsvetnoy-obodok-plan.md`: «почти нейтральная цель
заставляет подбор тянуться к более светлому и более насыщенному артикулу».

**Следствие для приёмки, важнее самого дефекта.** Воротный порог «уход тона ≤ 0,005 на
дельфине» меряет не качество раскладки, а на какую сторону границы палитры упал нейтральный
цвет. Он переворачивается от любой правки и **судьёй быть не может** — это ровно тот случай,
о котором правило «ложные метрики бывают» в `CLAUDE.md`. До того как дефект починен, ворота на
дельфине смотреть глазами (серая спина или нет), а не по числу.

**Что сделать.** Смотреть подбор для малой насыщенности: у почти нейтральной цели ошибка по
тону стоит дороже, чем по светлоте, а `ColorDistance` этого не знает. Кандидаты: штраф за
набор насыщенности при нейтральной цели; учёт того, что соседи по курсу выбрали (`CoherentMap`
это уже делает, но после того, как полоса выбрана целиком, и потому её не ломает).

**Где в коде.** `Core/Quantization/Quantizer.Map`, `Core/Colors/ColorDistance.cs`,
`Core/Quantization/CoherentMap.cs`.

---

## 10. Пайплайн молча выдаёт кашу, когда сюжет не влезает в размер

**Проблема.** Рабочий процесс — «нашёл картинку в интернете, сделал картон». `human` на 21×21
отрабатывает честно и выдаёт нечитаемое лицо, ни словом не предупредив, что 19 тессер поперёк
для портрета мало в принципе.

**Чем подтверждается.** Проверено 2026-09-05 на всех десяти кадрах, четырёх размерах и пяти
радиусах уплощения. `human` 21×21: на картоне бурая масса, тёмная тень и один розовый кусок
на месте рта — ни брови, ни глазницы, ни носа. На 40×40 тот же кадр читается полностью.
Никакой фильтр этого не чинит: деталей нужного масштаба в панно физически не существует.
Прежний пример (`landscape-2` 15×15) относился к кадру и размеру, которых больше нет.

**Что сделать.** Проверка на входе: сравнить масштаб значимой детали фотографии с размером
куска и сказать словами — «этому сюжету нужно от 40 см либо более тесный кроп». Это,
вероятно, полезнее для дела, чем следующий этап обработки: чинит выбор кадра, а не картон.

**Приёмка.** Проверка обязана пропустить `gull`/`dolphin` на 21×21 (они там читаются) и
остановить `human` на 21×21 — иначе она либо бесполезна, либо мешает.

**Где в коде.** Новая проверка рядом с `Core/Validation/MosaicRequestValidator.cs`, но с
доступом к фотографии; предупреждение показывать в `Web/Views/Mosaic/`.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 40×40 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Печать больше A4 пока не планируется, но склейка понадобится: разрезка на
листы с перекрытием, метками совмещения и номером листа; лучше PDF, чтобы масштаб не зависел
от просмотрщика.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 40×40 см →
4800 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.

---

## 11. Мелочи и техдолг

- **Тензор считается дважды за прогон** — флаттенером по сырому фото, `DirectionField` по
  уплощённому. Это намеренно (разные картинки, разные вопросы), не дубль; записано, чтобы
  никто не «оптимизировал».

---

## 13. Редуктор палитры создаёт разнобой, который потом чинит второй проход `CoherentMap`

**Проблема.** Когда `PaletteReducer` роняет артикул, его осиротевшие ячейки раздаются по
оставшимся артикулам через `Quantizer.NearestIndex` — **по одной ячейке, без учёта того, что
выбрали её соседи** (`Core/Quantization/PaletteReducer.cs:133–142`, метод `Reduce`). Именно
здесь заново создаётся то самое рассогласование с соседями, которое первый проход
`CoherentMap.Settle` (до редукции) только что убрал.

**Чем подтверждается (диагноз, а не догадка).** Замер 2026-09-05, 33 прогона, рука с одним
проходом `CoherentMap` после редукции против руки с двумя: без второго прохода одиночки
0,63 → 0,92 %, громкие 0,41 → 0,61 %, острова размером 1–2 — 1,60 → 2,05 %; на картоне
`dolphin` 40×40 без второго прохода белая тессера садится в тёмную полосу на спине. То есть
**второй проход `CoherentMap.Settle` убирать нельзя** — он делает настоящую работу, и именно
это доказывает, что редуктор рассогласование действительно порождает, а не то, что второй
проход просто перестраховка.

**Почему это техдолг, а не устройство пайплайна.** Два прохода одного и того же алгоритма —
подозрительный паттерн сам по себе (см. правило «встретил несогласованность — почини или
запиши» в `CLAUDE.md`), а раз без второго прохода метрики измеримо хуже, значит первый проход
недоделал свою работу, и её приходится переделывать заново после редукции.

**Что сделать.** Внутри `PaletteReducer.Reduce`, в цикле `foreach (int cell in orphans)`
(`PaletteReducer.cs:133`), заменить точечный `Quantizer.NearestIndex(cellLab[cell], candidateLab)`
на выбор, учитывающий уже осевшие соседние ячейки — в духе того, что делает
`CoherentMap.Settle`, но для одной осиротевшей ячейки сразу в момент раздачи, а не отдельным
проходом по всему полю после. Кандидаты: звать `CoherentMap` точечно для каждой ячейки-сироты
сразу после переприсвоения; либо копить `orphans` и решать их не строго по одной, а с оглядкой
на соседей, уже получивших свой артикул в этом же раунде редукции.

**Это исследовательская правка алгоритма** (`CLAUDE.md`, раздел «Как работаем»): нужен план в
`docs/*.md` с обоснованием и способом перепроверки, прежде чем трогать код — приёмка по всем
фото из `samples/`, обе руки, воротные кадры, «судить по картону, а не по метрике».

**Где в коде.** `Core/Quantization/PaletteReducer.cs` (сам дефект),
`Core/Quantization/CoherentMap.cs` (образец логики, которую надо перенести),
`Core/Pipeline/MosaicGenerationService.cs:106–120` (оба вызова `CoherentMap.Settle`, до и после
редукции — второй пока не трогать). Разбор руки с одним проходом —
`docs/chetyre-sglazhivatelya-plan.md`, раздел про руку E.

---

## 14. `MaxColors` внутри пайплайна значит две разные вещи

**Проблема.** Пользователь указывает в форме одно число — «не больше X цветов». Внутри
пайплайна это же число (`request.MaxColors`) используется дважды с разным смыслом:

- в `Core/Quantization/PaletteReducer.cs`, `Reduce(..., maxColors, ...)` — жёсткий потолок числа
  артикулов, ровно то, что ожидает пользователь;
- в `Core/Quantization/ToneMap.cs`, `IntoPaletteRange(..., shadeCount)` — оценка «насколько
  мелкий тональный шаг вообще можно различить», откуда берётся ширина растяжки:
  `step = (paletteHigh - paletteLow) / shadeCount` (`ToneMap.cs:72`). Оба вызова —
  `MosaicGenerationService.cs:89` и `:109` — получают один и тот же `request.MaxColors`.

**Почему это плохо, а не просто внутренняя деталь.** Смысл `ToneMap` — не про число артикулов,
а про то, насколько сильно растягивать тона фотографии. Сейчас смена «12 цветов» на «15» в
форме меняет ещё и силу растяжки — то есть весь общий контраст и распределение тонов на
картоне, — хотя пользователь просил только другое число артикулов. Пользователь не должен
догадываться про тональные шаги: ему нужно только «не больше X цветов», и форма меняться не
должна.

**Что сделать.** В вебе и в `MosaicRequest.MaxColors` — ничего не менять, это по-прежнему
единственное число, которое видит пользователь. Внутри пайплайна развязать: `PaletteReducer`
продолжает получать `MaxColors` как потолок артикулов; `ToneMap` должен получать свою оценку
шага растяжки из чего-то другого — вероятный кандидат: сколько тональных ступеней реально
держит сама палитра (`paletteLab`), независимо от того, сколько артикулов разрешил пользователь
на этот прогон.

**Это исследовательская правка** — нужно проверить, что именно подставить вместо
`request.MaxColors` в `ToneMap.IntoPaletteRange`, и не сломать ли это растяжку на прогонах, где
сейчас всё нормально (в частности воротные кадры `human`, `gull`, `dolphin`). План в `docs/*.md`
и замер по `samples/`, как и для любой правки алгоритма — прежде чем трогать код.

**Где в коде.** `Core/Quantization/ToneMap.cs` (`IntoPaletteRange`, параметр `shadeCount`),
`Core/Quantization/PaletteReducer.cs` (`Reduce`, параметр `maxColors` — этот смысл не трогать),
`Core/Pipeline/MosaicGenerationService.cs:89,109` (точка, где оба вызова расходятся).
