## Приёмка

Процедура, на которую ссылается `CLAUDE.md`. Держится только здесь.

**Прогон.** Все фото из `samples/` (10 штук), три размера, откус 10 мм, 12 цветов —
30 прогонов, около трёх секунд каждый, весь набор — две минуты:

```bash
for f in samples/*.jpg; do n=$(basename $f .jpg); \
  dotnet run -c Release --project tools/MosaicGenerator.Diag -- \
    --photo $f --out diag-out/priyomka/$n --sizes 21x21,21x29.7,40x40 \
    --modules 10 --colors 12; done
```

Мерить по всем десяти, а не по трём: настройки однажды оказались подогнаны под `gull.jpg`
(`docs/diagnostika-chayka.md`). Сравнение — только «до/после на одном кадре»: снять базу
тем же прогоном **до** правки, дальше сравнивать построчно по паре (фото, размер).

**База 2026-09-07**, снята после правки п. 4б и сверки палитры по вееру (`b4dc27b`).
Прежние цифры ΔE несравнимы — база сброшена вместе с HEX.

| колонка | медиана | p90 | max | что ловит |
|---|---|---|---|---|
| `jointArea` | 0,2354 | 0,2440 | 0,2474 | поле клея между курсами; номинал 0,205 для куска 10×7 со швом 1 мм |
| `narrowWidth` | 0,0624 | 0,1211 | 0,1519 | доля кусков уже 5 мм — руками не отколоть |
| `minWidthP5` | 4,90 мм | 5,47 | 5,60 | узкая сторона по нижнему хвосту |
| `filler` | 0,0572 | 0,0841 | 0,0926 | рябь из затычек в фоне |
| `awkward` | 0,0338 | 0,0721 | 0,0778 | неудобные для откуса куски |
| `kink` | 0,0978 | 0,1506 | 0,1848 | излом курса |
| `overlap` | 0,0009 | 0,0017 | 0,0022 | куски заезжают друг на друга |
| `bare03` | 0,0025 | 0,0074 | 0,0086 | голое поле больше трети модуля |
| `dE_mean` | 10,38 | 15,60 | 22,82 | уход цвета; дно задаёт палитра, а не алгоритм |
| `smallIsland` / `midIsland` | 0,0128 / 0,0701 | 0,0324 / 0,1420 | 0,0512 / 0,1813 | крап: острова 1–2 и 3–10 кусков |
| `merged` | 0,4391 | 0,5126 | 0,6652 | доля переназначенного при редукции |

**Сорванные ворота — по числам.** Любое из этого останавливает правку:

- `uncuttable`, `manySided`, `concave` перестали быть нулями. Сейчас все три — ровно 0
  во всех 30 прогонах; кусок, который не выкусывается, семиугольник и вогнутый кусок
  запрещены физикой материала, а не вкусом.
- `narrowWidth` вырос по медиане, или `minWidthP5` упал ниже 4,5 мм — панно набирается
  крошевом, которого руками не наколоть.
- `bare03` или `overlap` выросли вдвое — куски не сходятся или лезут друг на друга.
- Контроль `sunset` (плоское поле, курсы прямые и параллельные) уехал больше чем на 0,001
  по `jointArea`: сейчас 0,2017 / 0,2053 / 0,2030 при номинале 0,205. Если сдвинулся он —
  правка задела не кривизну, а саму резку.

**Ворота глазами — картон, а не число** (`CLAUDE.md`, «Судья — картон, а не метрика»).
`Read` по PNG из прогона, до и после, на одном кадре:

- `gull` 21×21 — клюв должен читаться пятном, фон вокруг силуэта без треугольных клиньев;
- `dolphin` A4 — **спина серая**, не бежевая и не фиолетовая. Смотреть глазами: воротный
  порог «уход тона ≤ 0,005» судьёй быть не может, см. п. 9б;
- `human` 40×40 — щека и плечо без синих и белых островков;
- `poppy` 21×21 — лепестки красные, не лососевые (сюда упирается `dE_mean` 22,8: это
  потолок веера, а не дефект раскладки);
- `sunset` 40×40 — контроль: курсы прямые, ни одного клина.

**Что не является воротами.** `v4` (доля четырёхугольников) — падает всякий раз, когда
клин заменяется выпуклым пятиугольником, то есть при улучшении; смотреть его только вместе
с `awkward` и `minWidthP5`. «Курсов < 3 кусков» — не дефект (проверено 2026-09-04).
«Уход тона на дельфине» — п. 9б. `merged` 0,44 — дно от 12 оттенков.

---

## 3. Крап: синий и почти белый островки по 6–12 кусков на лице

**Что осталось.** На картоне `human` 40×40 в тени щеки и на плече — островки по 6–12 кусков.
Не одиночка: внутри острова у куска есть одноартикульные соседи, и ветка одиночки в
`CoherentMap` их не трогает намеренно. Одиночки и пары закрыты 2026-09-06
(`docs/krap-odinochki-plan.md`): `human` 40×40 одиночки 0,8 → 0,4 %, острова 1–2 — 2,8 → 1,8 %.

**Фикс не строим — нет видимой цели.** Мерка есть (`midIsland`, острова 3–10), но связка
«кластеры-двойники + честная раздача сирот» её не двинула (`human` 40×40: 0,116 → 0,116),
а медиана по набору 0,070 — метрика ловит массу законных мелких пятен. Отдельного «блоба»
особняком от нормальной структуры глазами не выделено (синее у макушки — рисунок на колпаке
из фото). Жёсткий median-проход по островам 3–10 отвергнут: при такой медиане съест детали.

**Если блоб всплывёт** — чинить в `PaletteReducer` (порог площади: не дробить прохладную тень
на отдельный артикул), а не проходом по карте.

**Где в коде.** `Core/Quantization/PaletteReducer.cs`, `Core/Quantization/CoherentMap.cs`,
`tools/MosaicGenerator.Diag/Metrics.cs` (`NeighbourArticles`).

---

## 4. Линия стыка двух курсов считается односторонне: клинья и нахлёсты

Один дефект с двумя знаками. Где линии реза двух курсов расходятся — между ними клин
(«стрелки» и «галочки» на картоне, лишек `jointArea` над номиналом). Где сходятся — кусок
заезжает на соседа. Причина общая: **линию стыка каждый кусок выбирает себе сам.** Прежний
п. 4б влит сюда 2026-09-07.

**Механизм — невзаимность в двух местах** (`Tessellation.CutCells`):

1. критерий «это стык курсов» — `Math.Abs(across) > span * 0.6` — считается по касательной
   **своего** куска. У пары A→B он срабатывает, у B→A может не сработать: A режет себя прямой,
   B — биссектрисой, линий получается две;
2. линия берётся от **ближайшего** куска соседнего курса (словарь `seams`). Ближайший для A —
   B, а ближайший для B — уже C, и линии расходятся по наклону и по отступу.

Прямая здесь правильна и биссектрисой не заменяется: мастер проводит линию и колет к ней,
поэтому смальта выходит четырёхсторонней. Замена на биссектрису убирает нахлёст почти в ноль
(max 14,6 → 0,94 мм²), но возвращает соты — проверено, отвергнуто.

**Сделано.** 2026-09-06 (`133604f`, `docs/dyry-kliny-plan.md`): `SeamCut` режет обе стороны
стыка **одной** линией с наклоном в полугла между курсами, но не круче `MaxSeamLean` = 3°.
Лишек `jointArea` над номиналом упал на четверть. 2026-09-07: критерий требует `CourseId >= 0`
с обеих сторон — филлер, у которого курса нет и касательная случайна, режется биссектрисой,
как со своими; `narrowWidth` 0,0911 → 0,0624, `overlap` вдвое, `awkward` 0,0554 → 0,0338.

**Что осталось — клин на торце курса. План: `docs/klin-na-tortse-kursa-plan.md`** (диагностика
проведена 2026-09-07, код не начат).

Адрес дефекта установлен замером по всем десяти кадрам: **торец курса заливки, медиана ×1,8**
клиньев к своей доле панно (структурный курс ×1,4, середина курса ×0,6, филлер ×0,0 — после
правки филлеров он перестал быть источником). На `gull` и `wave` вперёд выходит структурный
курс, но это два кадра из десяти: судить по ним — переобучение на `gull`.

Механизм: курсы обрываются **по расстоянию до соседа в 97–100 %** случаев. Трассировка встаёт,
когда центральная линия подходит на `dTest` = 4 мм к чужой; под углом схождения θ этот зазор
растягивается вдоль курса в 4/sin θ — при 20° это 11,7 мм, больше модуля. `sunset` чист
не из-за резки, а потому что там курсы параллельны и упираются в край.

**Опровергнуто замером, не повторять.** `dTest` 0,50 → 0,35 → 0,25: `jointArea` не двинулся
(0,2322 / 0,2366 / 0,2320), точек зазора стало больше (2200 → 2913), торец стал хуже
(×1,6 → ×2,1). Порог не трогать. Ранее так же отпали `SquareOff` (`Restore = 0` — нахлёст
217 → 235 мм², узких 10,8 → 12,5 %), симметризация критерия через ИЛИ (нахлёст 145 → 191 мм²,
голое поле 0,02 → 0,20 %, дыра 0,33 → 0,50 модуля) и сплошная биссектриса (возвращает соты).

**Первый шаг — не правка, а мерка дна.** В клин под острым углом кусок с узкой стороной ≥ 5 мм
физически не поставить, и мастер оставляет там клей. Значит у `jointArea` есть дно, заданное
материалом, и оно не измерено; пока не измерено — цель по этой колонке ставить нельзя.
План вводит `wedgeLegal` / `wedgeIllegal` и допускает честный исход «чинить нечего, снять
`jointArea` с роли судьи».

**Как увидеть глазами.** Карта `--holes` в стенде (`tools/MosaicGenerator.Diag/HoleMap.cs`) —
единственный способ смотреть этот дефект, а не колонку CSV. Попарного аудита нахлёста
в репозитории нет: он писался временным файлом, полсотни строк, писать заново.

**Где в коде.** `Core/Grid/Tessellation.cs` — `CutCells` (критерий стыка, словарь `seams`),
`SeamCut`, `MaxSeamLean`; `Core/Grid/FieldGeometry.cs` — `ClipToBisector`, `ClipToLine`,
`SquareOff`.

---

## 9б. Почти нейтральный цвет перекидывается между серым и бежевым артикулом

**Проблема.** Серо-мау́вовое тело дельфина стоит ровно на границе между серым артикулом и
бежевым. Любое возмущение где угодно выше по пайплайну перекидывает через границу целую
полосу тессер, и по спине идёт бежевая или фиолетовая полоса. Видно не «оттенок чуть мимо»,
а другой цвет животного.

**Чем подтверждается.** Уход тона на `dolphin` по радиусу уплощения скачет между нулём и тремя
процентами без тренда, на 21×21 и A4 в противофазе. Глазами: A4 на радиусе 1,2 — бежевая
полоса по спине и фиолетовое тело; он же на 1,5 — чистое серое. Родственный разбор —
`docs/tsvetnoy-obodok-plan.md`.

**Следствие для приёмки важнее самого дефекта.** Порог «уход тона ≤ 0,005 на дельфине» меряет
не качество раскладки, а на какую сторону границы палитры упал нейтральный цвет. Судьёй быть
не может — см. раздел «Приёмка»: спина дельфина смотрится глазами.

**Что сделать.** У почти нейтральной цели ошибка по тону стоит дороже, чем по светлоте, а
`ColorDistance` этого не знает. Кандидаты: штраф за набор насыщенности при нейтральной цели;
учёт того, что выбрали соседи по курсу (`CoherentMap` это делает, но уже после того, как
полоса выбрана целиком, и потому её не ломает).

**Где в коде.** `Core/Quantization/Quantizer.Map`, `Core/Colors/ColorDistance.cs`,
`Core/Quantization/CoherentMap.cs`.

---

## 18. Авто-подбор числа цветов + лесенка картонов на лету (план готов, код не начат)

**Задача.** Число цветов подбирается вручную полной перегенерацией: у процента переназначения
из строки редукции есть эмпирический смысл (5–7 % — сюжет цел, >10–15 % — сыпется), но
проверять каждую пробу дорого.

**Статус 2026-09-07.** Не начат: `ReductionLadder.cs` не существует, колонок `autoColors` /
`kneeRatio` в стенде нет. Есть только план.

**Решение** (`docs/avto-podbor-cvetov-plan.md`). `PaletteReducer.BuildLadder` — вся лесенка
стоимости за один вложенный проход; `ReductionLadder.Knee` ищет излом предельной стоимости
сброса (`KneeFactor = 3.5`, калибруется по `samples/`). `Generate` считает префикс пайплайна
один раз, печёт картоны и расход для диапазона авто ±5; веб держит стрелки ◄► над картоном —
мгновенная подмена в печёном диапазоне, «пересчитать» за его пределами. Стенд: колонки
`autoColors` / `kneeRatio`.

**Где в коде.** `Core/Quantization/ReductionLadder.cs` (новый), `Core/Quantization/PaletteReducer.cs`,
`Core/Pipeline/MosaicGenerationService.cs` + `MosaicResult.cs`, `Web/Services/TempResultStore.cs`
+ `StoredResult.cs`, `Web/Controllers/MosaicController.cs`, `Web/Views/Mosaic/Result.cshtml`
+ `_Controls.cshtml`, `Web/wwwroot/js/ui.js`, `tools/MosaicGenerator.Diag/Program.cs` + `Metrics.cs`.

---

## 21. Обратный набор: картон и схему некому зеркалить

**Проблема.** При обратном наборе тессеры кладутся лицом вниз на временную основу поверх
картона, и готовая работа выходит **зеркальной** тому, что напечатано. Зеркального вывода нет:
`grep` по `src`, `tools`, `docs` на mirror / зеркал / flip не находит ничего. Весь рендер
спроектирован под прямой набор, это записано в `RenderOptions.Cartoon`: «printed 1:1 and set
on directly».

**Чем подтверждается.** 2026-09-07: работа 20×20 см по `gull-2` набирается обратным способом,
печать 1:1 на A4. На симметричном сюжете расхождение не видно, на чайке клюв развернётся
в другую сторону — и поправить после переноса нечем.

**Что сделать.** Флаг в `RenderOptions` — не настройка на каждый запуск, а способ набора, он
у мастера один на работу. Зеркалится **геометрия**; номера на схеме и линейка 100 мм рисуются
поверх уже отражённого поля и не зеркалятся, иначе схема нечитаема. Легенда не зеркалится
вовсе — отдельный лист. Проверка: наложить зеркальный картон на прямой — совпадение по
вертикальной оси, и все цифры на схеме читаются слева направо.

**Где в коде.** `Core/Rendering/RenderOptions.cs`, `Core/Skia/SkiaMosaicRenderer.cs`
(`RenderCartoon`, `RenderScheme`), `Core/Rendering/CartoonSheet.cs` (линейка),
`Web/Controllers/MosaicController.cs` + форма.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 40×40 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Понадобится: разрезка на листы с перекрытием, метками совмещения и номером
листа; лучше PDF, чтобы масштаб не зависел от просмотрщика. 20×20 см влезает на A4 целиком,
так что до панно крупнее A4 пункт не горит.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 40×40 см →
4800 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.
