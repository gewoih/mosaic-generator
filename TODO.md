## 3. Крап в островах 3–10 кусков: синий/белый блоб на лице (одиночки закрыты 2026-09-06, мерка добавлена 2026-09-06)

**Мерка добавлена 2026-09-06** (`docs/redukciya-svyazka-plan.md`, шаг 3): колонка `midIsland`
в стенде — доля кусков в связных одноартикульных островах 3–10. Замер: связка «кластеры-двойники
+ честная раздача сирот» (`docs/redukciya-svyazka-plan.md`) `midIsland` на `human` 40×40 не
двинула (0,116 → 0,116), медиана по набору 0,076 — метрика ловит массу законных мелких пятен.
Отдельного «блоба» особняком от нормальной структуры на картоне `human` 40×40 глазами не
выделено (синее у макушки — картинка на колпаке из фото).
**Фикс не строим** — нет видимой цели; мерка на месте, если блоб всплывёт. Жёсткий
median-проход по островам 3–10 отвергнут: при медиане 0,076 съест реальные детали.

**Одиночки и пары закрыты 2026-09-06** (`docs/krap-odinochki-plan.md`, вариант A′).
`CoherentMap` получил ветку для громкой истинной одиночки: кусок без единого одноартикульного
соседа, чей артикул дальше 10 ΔE от артикула каждого соседа, снимается на моду соседей
(`IsolationNeighbourWeight`, `IsolationMinRing`, `IsolationLoudGap`). Замер по всем `samples/`:
`human` 40×40 одиночки 0,8 → 0,4 %, громкие 0,4 → 0,2 %, острова 1–2 — 2,8 → 1,8 %; `dolphin`
A4 одиночки 0,7 → 0,1 %; сиреневые одиночки в воде и синий крап на макушке `human` ушли,
ворота (клюв чайки пятном, спина дельфина серая) целы. Цена: `merged` +0,01–0,02 в трети
строк, `gull` A4/40×40 +2–5 кусков в одиночках (на картоне не читается).

**Что осталось.** На картоне `human` 40×40 в тени щеки/на плече — **синий и почти белый
островки по 6–12 кусков**. Это не одиночка (внутри острова у куска есть одноартикульные
соседи), механизм п. 3 их не трогает намеренно. Дефект ближе к:

- **п. 5** — прохладный теневой артикул, который следовало слить с соседним, но `PaletteReducer`
  оставил как отдельный, потому что кусков набралось на «блоб»;
- **раздача сирот в редукторе** — она может собрать из осиротевших ячеек связное пятно не на
  своём месте. С 2026-09-06 раздача идёт с оглядкой на соседей (`PaletteReducer.HandOutOrphans`,
  `docs/redukciya-svyazka-plan.md`), и `midIsland` на воротном `human` 40×40 это не двинуло.

**Чем подтверждается** (33 прогона, `posle/` 2026-09-06). Доля кусков в островах одного
артикула размером 1–2 (одиночки почти обнулены):

| фото / размер | в островах 1–2 |
|---|---|
| human 21×21 | **3,5 %** |
| human A4 | 3,5 % |
| wave A4 | 3,1 % |
| landscape A4 | 1,8 % |
| human 40×40 | 1,8 % |

**Что сделать.** ~~Мерка островов 3–10 в стенде~~ — сделано (`midIsland`). Замер связки
по `docs/redukciya-svyazka-plan.md` её не двинул и видимого блоба не показал, поэтому фикс отложен: нужна явная
цель на картоне. Если появится — чинить не жёстким median-проходом (съест детали при
медиане 0,076), а в `PaletteReducer` (порог площади: не дробить прохладную тень на отдельный
артикул).

**Где в коде.** `Core/Quantization/PaletteReducer.cs`, `Core/Quantization/CoherentMap.cs`
(ветка одиночки уже есть — расширять на связные пятна рискованно: съест мелкие детали),
`tools/MosaicGenerator.Diag/Metrics.cs` (`NeighbourArticles` — `midIsland` уже считается).

---

## 4. Шов шире номинала: клинья на стыке непараллельных курсов (уменьшено на четверть 2026-09-06)

**Переформулирован 2026-09-06** (`docs/dyry-kliny-plan.md`). Прежняя формулировка — «дыры до
7,2 мм» — ловила хвост: площадь под зазором > 3 мм это 0,0–0,6 % панно. Тело дефекта в другой
колонке: `jointArea` — **0,24–0,27 при номинале 0,205** для куска 10×7 со швом 1 мм. Глаз
читает как «стрелки» и «галочки» не отдельную дыру, а поле треугольного клея между рядами.

**Механизм** (карта `--holes` в стенде, `HoleMap.cs`). Кусок из соседнего курса отрезался
прямой, параллельной **своему** курсу; кусок с той стороны стыка — прямой, параллельной
**его** курсу. Курсы под углом θ — линии реза расходятся под тем же углом, между ними клин.
Контроль: `sunset` (плоское поле, курсы прямые и параллельные) во всех прогонах даёт
`jointArea` 0,2005 и ни одного клина. Корреляции: `jointArea` ~ `cut` r = +0,96, с числом
тессер r = +0,01 — площадь шва задаёт криволинейность, а не мелкость куска.

**Сделано.** `Tessellation.SeamCut`: обе стороны стыка режутся **одной** линией — она
наклонена на половину угла между курсами, но не больше `MaxSeamLean` = 3° (дальше кусок
превращается в косой обрезок: при полной биссектрисе `minWidthP5` 4,5 → 2,4 мм). Линия
берётся одна на курс, от ближайшего его куска, — не по одной на каждого соседа, иначе кусок
режется десятком слегка разных полуплоскостей и обгрызается.

**Приёмка** (33 прогона, все `samples/`): `jointArea` 0,2534 → **0,2405**, `covered`
0,7439 → 0,7562, `narrowWidth` 0,1212 → **0,0914**, `filler` 0,0802 → 0,0676, `dE_mean`
9,49 → 9,38; `v4` −0,0006, `uncuttable` 0,0000, контроль `sunset` в пределах 0,001. Ворота
глазами целы. Регрессии: `narrowWidth` на `poppy` и `wave` 21×21 (два прогона из 33).

**Что осталось.** Лишек над номиналом был 0,048 доли панно, стал 0,035 — **четверть**.
`jointP90` (1,71 мм) и `jointMax` (5,50 мм) не двинулись: потолок 3° по построению оставляет
клин везде, где курсы скрещиваются круче 6°. На картах после правки оставшиеся клинья сидят
там, где один курс **обрывается** о другой — это, возможно, уже вопрос к посеву курсов, а не
к резке. Продолжение — вариант C из плана (не давать курсам сходиться, правка укладчика),
отдельным планом и только под видимую на картоне цель.

**Где в коде.** `Core/Grid/Tessellation.cs` (`SeamCut`, `CutCells`),
`tools/MosaicGenerator.Diag/HoleMap.cs` + флаг `--holes` — единственный способ увидеть этот
дефект глазами, а не через колонку CSV.

---

## 4б. Куски заезжают друг на друга на стыке курсов и вокруг филлеров

Обратная сторона пункта 4: там линии реза двух курсов расходятся и между ними остаётся клин,
здесь они сходятся внахлёст и кусок заезжает на соседа. Причина одна и та же — линию стыка
каждый кусок выбирает себе сам.

**Замерено 2026-09-06.** Прогон:

```bash
dotnet run -c Release --project tools/MosaicGenerator.Diag -- \
    --photo samples/gull.jpg --sizes 40x40 --modules 10 --colors 12
```

`gull` 40×40, кусок 10×7 мм, шов 1 мм, 36 поперёк, 1906 тессер (1,06× от номинала), 142 курса.

**Мерка.** Колонка `overlap` в `metrics.csv` (`CoverageMask.OverlappedFraction`) — доля
пикселей растра, накрытых больше чем одним куском: **0,14 % поля**. Дефект она прячет, потому
что площадная, а глаз ловит **один** заехавший кусок. Настоящая мерка — попарное пересечение
полигонов: для каждой пары ближе трёх модулей клип Сазерленда—Ходжмена одного полигона другим
и площадь по шнуровке. Такой аудит писался временным файлом в стенде и в репозиторий не
клался; если возвращаться к пункту — писать заново, полсотни строк.

**Что показал аудит на прогоне как есть:**

- пар с нахлёстом **251**, задет **21,6 % кусков** (411 из 1906);
- на пару: p50 **0,24 мм²** (меньше шва, глазом не видно), p90 **2,0 мм²**,
  max **14,6 мм² — пятая часть куска 10×7**;
- по площади: разные курсы **103 мм²**, с филлером **113 мм²**, внутри курса **1 мм²**,
  всего 217 мм² = 0,138 % поля.

**Механизм — невзаимность реза.** Внутри курса и между филлерами куски режутся биссектрисой
(`FieldGeometry.ClipToBisector`): она по построению одна на пару, наложиться нечем — те самые
13 пар и 1 мм² численного шума. На стыке двух курсов работает `SeamCut` — прямая линия, и это
правильно (пункт 4: мастер проводит линию и колет к ней, биссектриса давала соты). Но решение
о ней принимается **односторонне**, в двух местах сразу:

1. критерий «это стык курсов» — `Math.Abs(across) > span * 0.6`, `Tessellation.cs:517` —
   считается по касательной **своего** куска. У пары A→B он срабатывает, у B→A может не
   сработать: тогда A режет себя прямой, а B — биссектрисой, и линий получается две;
2. линия берётся от **ближайшего** куска соседнего курса (словарь `seams`,
   `Tessellation.cs:483–488`). Ближайший для A — B, а ближайший для B — уже C, и линии
   расходятся по наклону (`lean` считается от касательных пары) и по отступу (`gap / 2`).

Пункт 4 (коммит 133604f) сделал линию взаимной **внутри одной пары**; невзаимным остался сам
выбор пары.

**Проверено экспериментом** (правка — замер — откат, всё на одном кадре `gull` 40×40):

| прогон | нахлёст, мм² | курсы | филлер | max | тессер | голое>0,3м | дыра | узких<5мм |
|---|---|---|---|---|---|---|---|---|
| как есть | 217 | 103 | 113 | 14,6 | 1906 | 0,02 % | 0,33м | 10,8 % |
| `SquareOff` без восстановления угла (`Restore` 0,4 → 0) | 235 | 92 | 144 | 14,6 | 1937 | 0,03 % | 0,33м | 12,5 % |
| `SeamCut` выключен, все соседи по биссектрисе | **50** | 29 | 0 | **0,94** | 1777 | 0,00 % | 0,33м | 2,1 % |
| критерий убран совсем (разные курсы — всегда прямая) | 567 | 24 | **542** | 23,4 | 2127 | **5,69 %** | 0,87м | 20,5 % |
| филлер режется биссектрисой, курсы как есть | **145** | 130 | **2,4** | 14,7 | 1905 | 0,01 % | 0,33м | **9,3 %** |
| критерий симметризован через ИЛИ | 191 | 148 | 30 | 13,6 | 2052 | 0,20 % | 0,50м | 13,6 % |

Выводы из таблицы:

- **`SquareOff` не виноват.** Гипотеза «восстановление угла на 0,4 пути к пересечению
  (`FieldGeometry.cs:230`) выпячивает кусок за его клетку» опровергнута: с `Restore = 0`
  нахлёст не упал, а вырос (217 → 235), зато узких кусков стало больше (10,8 → 12,5 %).
  Восстановление угла трогать незачем.
- **Виноват `SeamCut`.** Стоит заменить прямую биссектрисой — крупные нахлёсты исчезают
  (max 14,6 → 0,94 мм², то есть остаётся только шум округления). Внедрять это нельзя:
  биссектриса возвращает соты, ради которых и делался пункт 4.
- **Половина беды — филлеры.** У филлера нет курса, касательная у него случайная, а прямую он
  всё равно получает — и получает не ту, что его сосед. Убрать критерий вовсе (всегда прямая
  между разными курсами) — филлерный нахлёст взрывается 113 → 542 мм², голое поле
  0,02 → 5,69 %.

**Дешёвая половина, не внедрена.** Требовать в критерии `CourseId >= 0` с обеих сторон —
филлер знакомится с соседями по биссектрисе, как со своими: нахлёст с филлерами
113 → **2,4 мм²** (87 → 17 пар), всего 217 → 145 мм², `narrowWidth` 10,8 → **9,3 %**; дыра,
голое поле, ΔE (7,5/18,4), обрыв, слипание — без изменений, тессер 1906 → 1905. Осталось
только прогнать приёмку по всем `samples/`: цифра снята на одном фото, а по CLAUDE.md так
однажды уже переобучились на `gull`.

**Полное лечение — правка укладчика, нужен план в `docs/`.** Считать линию стыка один раз
**на пару курсов** в окрестности, а не на кусок, чтобы обе стороны резались тождественной
прямой. Дешёвая симметризация уже проверена и не годится: критерий через ИЛИ убирает часть
межкурсовых пар (180 → 94), но общую площадь не снижает (145 → 191 мм²), плодит филлеры и
поднимает голое поле 0,02 → 0,20 %, дыру 0,33 → 0,50 модуля.

**Где в коде.** `Core/Grid/Tessellation.cs` — `CutCells` (критерий стыка и словарь `seams`),
`SeamCut` (`:1350`), `MaxSeamLean`; `Core/Grid/FieldGeometry.cs` — `ClipToBisector`,
`ClipToLine`, `SquareOff`. Смежные пункты: 4 (клинья — та же причина, другой знак).

---

## 9б. Почти нейтральный цвет перекидывается между серым и бежевым артикулом

**Проблема.** Серо-мау́вовое тело дельфина стоит ровно на границе между серым артикулом и
бежевым. Любое возмущение где угодно выше по пайплайну перекидывает через эту границу целую
полосу тессер, и на картоне по спине идёт бежевая или фиолетовая полоса. Пользователь видит
не «оттенок чуть мимо», а другой цвет животного.

**Чем подтверждается.** Уход тона на `dolphin` по радиусу уплощения, тонкая сетка:

| размер | 0,7 | 0,85 | 1,0 | 1,2 | 1,35 | 1,5 |
|---|---|---|---|---|---|---|
| 21×21 | 0,000 | 0,000 | **0,030** | 0,000 | **0,022** | **0,026** |
| A4 | 0,000 | 0,000 | 0,004 | **0,024** | **0,027** | 0,000 |

Скачки между нулём и тремя процентами без тренда, на двух размерах в противофазе. Проверено
глазами: `dolphin` A4 на 1,2 — бежевая полоса по спине и фиолетовое тело; он же на 1,5 —
чистое серое. Родственный разбор — `docs/tsvetnoy-obodok-plan.md`: «почти нейтральная цель
заставляет подбор тянуться к более светлому и более насыщенному артикулу».

**Следствие для приёмки, важнее самого дефекта.** Воротный порог «уход тона ≤ 0,005 на
дельфине» меряет не качество раскладки, а на какую сторону границы палитры упал нейтральный
цвет. Он переворачивается от любой правки и **судьёй быть не может** — это ровно тот случай,
о котором правило «ложные метрики бывают» в `CLAUDE.md`. До того как дефект починен, ворота на
дельфине смотреть глазами (серая спина или нет), а не по числу.

**Что сделать.** Смотреть подбор для малой насыщенности: у почти нейтральной цели ошибка по
тону стоит дороже, чем по светлоте, а `ColorDistance` этого не знает. Кандидаты: штраф за
набор насыщенности при нейтральной цели; учёт того, что соседи по курсу выбрали (`CoherentMap`
это уже делает, но после того, как полоса выбрана целиком, и потому её не ломает).

**Где в коде.** `Core/Quantization/Quantizer.Map`, `Core/Colors/ColorDistance.cs`,
`Core/Quantization/CoherentMap.cs`.

---

## 12. Склейка листов A4 с метками (на будущее)

Картон 40×40 см в масштабе 1:1 на A4 не влезает. Сейчас на выходе один PNG с `pHYs`; резать и
склеивать — руками. Печать больше A4 пока не планируется, но склейка понадобится: разрезка на
листы с перекрытием, метками совмещения и номером листа; лучше PDF, чтобы масштаб не зависел
от просмотрщика.

**Где в коде.** `Core/Rendering/PngMetadata.cs` (масштаб пишется корректно: 40×40 см →
4800 px → 304,8 dpi), `Web/Controllers/MosaicController.Cartoon`.

---

## 16. Строка-читалка в форме считает сетку квадратным куском

**Проблема.** Живая читалка под размером панно (рельса управления) берёт длину откуса и
для колонок, и для рядов. Настоящая раскладка ставит поперёк курса кусок в толщину пластины
(больше откуса), поэтому для квадратного панно рядов меньше, чем колонок. Читалка обещает
`21 × 21 = 441`, а `Пересобрать` выдаёт `21 × 18 = 378` — расхождение видно рядом со сводкой
результата.

**Чем подтверждается.** 2026-09-06, `gull` 15×15 см, откус 6 мм: читалка «Сетка 21 × 21»,
сводка результата «21 × 18». `MosaicLayout.Compute` — `rows` от `ModuleHeightMm`
(поперечник = `Palette.TypicalThicknessMm`), `columns` от `ModuleWidthMm` (откус).

**Что сделать.** Отдать поперечник куска в `layout.js`. Он зависит от палитры, поэтому
`MosaicController.LayoutData()` должен класть `TypicalThicknessMm` (по выбранной палитре или
по всем), а `layout.js` — считать `rows` через `ModuleSelector.Choose`-эквивалент, а не через
длину откуса. До этого читалка на странице результата вводит в заблуждение.

**Где в коде.** `Web/Controllers/MosaicController.cs` (`LayoutData`),
`Web/wwwroot/js/layout.js` (`choose`), `Core/Domain/ModuleSelector.cs`.

---

## 18. Авто-подбор числа цветов + лесенка картонов на лету (в работе)

**Задача.** Мастер подбирает число цветов вручную полной перегенерацией: у процента
переназначения из строки редукции есть эмпирический смысл (5–7 % — сюжет цел, >10–15 % —
сыпется), но проверять каждую пробу дорого. Автоматизировать выбор и дать менять его на лету.

**Решение** (`docs/avto-podbor-cvetov-plan.md`). `PaletteReducer.BuildLadder` — вся лесенка
стоимости за один вложенный проход; `ReductionLadder.Knee` ищет излом предельной стоимости
сброса (`KneeFactor = 3.5`, консервативно, калибруется позже по `samples/`). `Generate`
считает префикс пайплайна один раз, печёт картоны + расход для диапазона авто ±5; веб держит
стрелки ◄► над картоном — мгновенная подмена в печёном диапазоне, «пересчитать» за его
пределами. Стенд: колонки `autoColors` / `kneeRatio` в `metrics.csv`.

**Где в коде.** `Core/Quantization/ReductionLadder.cs` (новый), `Core/Quantization/PaletteReducer.cs`,
`Core/Pipeline/MosaicGenerationService.cs`, `Core/Pipeline/MosaicResult.cs`,
`Web/Services/TempResultStore.cs` + `StoredResult.cs`, `Web/Controllers/MosaicController.cs`,
`Web/Views/Mosaic/Result.cshtml` + `_Controls.cshtml`, `Web/wwwroot/js/ui.js`,
`tools/MosaicGenerator.Diag/Program.cs` + `Metrics.cs`.

---

## 17. Легенда налезает сама на себя при части палитр

**Проблема.** На листе легенды строки с 5–6 позиции печатаются одна поверх другой
(`TJ01 B04 VB05 V807 VB07` в кашу), коды не читаются.

**Чем подтверждается.** 2026-09-06, `gull` 15×15, лимит цветов 8 и 12 — обе легенды бьются
в одном и том же месте. Видно и в вебе во вкладке «Легенда», и в скачанном PNG.

**Где в коде.** Генератор легенды в `Core/Rendering/` (лист легенды, раскладка колонок и
строк по числу оттенков).

---

## 19. Шесть белых артикулов неразличимы: расход выписывает случайный из них

**Проблема.** `GX02`, `VB04`, `VB06`, `VB07` записаны ровно как `#FFFFFF`, `VB05` отстоит от них
на dE76 0,54, `TJ01` — на 1,09. Для квантователя это один цвет: какой артикул попадёт в таблицу
расхода, решает порядок перебора, а не картинка. Мастер поедет в магазин за `VB06`, хотя набирал
бы `TJ01`. Тот же куст ломает и легенду (п. 17: `TJ01 B04 VB05 V807 VB07` печатаются в кашу).

**Чем подтверждается.** Сверка палитры с фотографией веера 2026-09-07 (`docs/palitra-sverka-po-veeru.md`):
на самом веере эти шесть плашек глазом тоже не различаются — это не ошибка снятия HEX, а факт
материала. Рядом тот же эффект помельче: `DK13`/`SU06` — 1,27, `FA01`/`FN10` — 1,69,
`SX56`/`SU06` — 1,86, `FA01`/`TX71` — 1,89.

**Что сделать.** Решение — не в HEX, а в правиле: держать в рабочей палитре один представитель
куста (для белых — `TJ01`, он в веере первый и уже используется), остальные пометить как
не участвующие в подборе. Это не «настройка», а вычёркивание — в духе «инструмент для себя».
Порог слияния взять по замеру: dE76 < 2 в веере надёжно не различается.

**Где в коде.** `Web/Data/palettes/artworker-smalt.json`, `Core/Domain/Palette.cs`,
`Core/Quantization/PaletteReducer.cs`.

---

## 20. `CLAUDE.md` ссылается на раздел «Приёмка», которого в `TODO.md` нет

**Проблема.** `CLAUDE.md` пишет: «Полная процедура — две руки, ворота глазами, что не должно
уехать — в `TODO.md`, раздел „Приёмка“. Держать её в двух местах нельзя: сюда только ссылка».
Раздела с таким заголовком в `TODO.md` нет — процедура либо потерялась при переработке файла,
либо живёт только в голове. Ссылка ведёт в пустоту, и очередной прогон приёмки собирается
заново каждый раз.

**Что сделать.** Восстановить раздел: набор фото (все `samples/`), размеры и число цветов,
воротные кадры и что на них смотрится глазами, какие числа считаются сорванными воротами.
Учесть, что часть порогов уже признана несудейской (п. 9б — уход тона на дельфине) и что
база ΔE сброшена 2026-09-07 сверкой палитры (`docs/palitra-sverka-po-veeru.md`).
